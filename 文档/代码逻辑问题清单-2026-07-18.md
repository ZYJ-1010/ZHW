# 代码逻辑问题清单（2026-07-18）

## 统计

- 总计：**32 项**。
- 已处理：**10 项**（P0-01 至 P0-10）。
- 待整改：**19 项**（P0 剩余 4 项、P1 高风险缺口及二期能力误开放 15 项）。
- 需要甲方确认后再处理：**3 项**（会员等级口径、好友私聊范围、局类型层级）。
- 本清单记录问题、证据与逐项修复进度；代码修改均按顺序单独提交本地仓库。

## 一、明确不符合（P0，14 项）

- [x] **P0-01 企业认证可被资料字段伪造（已改为独立人工审核流程）**
  - 需求：企业认证或对应资质必须经过真实审核。
  - 代码：`services/go-api/internal/appapi/role_qualification.go:50-58`；`profile_system_handler.go` 的 profile JSON 保存逻辑。
  - 问题：通过 `certifications` JSON 判断认证，没有独立认证实体、附件归属校验和后台审核链路。
  - 影响：用户可伪造 `approved/verified` 状态绕过角色申请条件。

- [x] **P0-02 领路人提交申请会把资格状态强制写成已满足（已移除，身份由后台白名单/导入直接授予）**
  - 代码：`services/go-api/internal/appapi/profile_handler.go:664-673`。
  - 问题：提交申请前直接写入 `ConditionMet=true`。
  - 影响：后台资格状态与真实审核状态不一致。

- [x] **P0-03 组局邀请接受后可能直接通过入局审核（已改为统一 pending，仍需最终审核）**
  - 代码：`services/go-api/internal/games/service.go:1110-1172`。
  - 问题：带 `InviteGroupID` 的邀请会把申请状态直接设为 `approved`。
  - 需求：接受邀请后仍须由发起人或行家最终审核。

- [x] **P0-04 普通玩家仍可生成组局内邀请（已限制外部邀请接口仅行家/领路人）**
  - 代码：`services/go-api/internal/appapi/server.go:1187-1194`；`services/go-api/internal/games/service.go:1043-1045`。
  - 问题：仅限制普通邀请码；组局内邀请允许创建者直接生成，即使创建者是普通玩家。
  - 需求：只有行家、领路人可生成邀请二维码/邀请入口。

- [x] **P0-05 入局审核只允许组局创建者（已支持主行家审核）**
  - 代码：`services/go-api/internal/games/service.go:1260-1262`。
  - 问题：没有放行 `MainGuideUserID`。
  - 需求：发起人或行家均可审核入局。

- [x] **P0-06 满员后没有后端自动创建唯一 IM 房间（已补充满员回调并复用唯一房间）**
  - 代码：`services/go-api/internal/games/service.go:1290-1297`。
  - 问题：满员只更新局状态，没有自动建房任务或唯一房间状态流转。

- [x] **P0-07 未成团局访问详情/IM 会提前建房（已增加成团状态门禁）**
  - 代码：`services/go-api/internal/appapi/game_handler.go:2543-2560`；`services/go-api/internal/im/service.go:260-293`。
  - 问题：成员访问详情时调用 `RoomForGame`，会懒创建房间，不判断是否成团。

- [x] **P0-08 IM 创建失败被伪装成 active 本地房间（已落库 create_failed 并保留后台重试）**
  - 代码：`services/go-api/internal/im/service.go:216-257`。
  - 问题：外部同步失败后仍落为 `engine=local,status=active`，没有 `create_failed` 和重试状态。

- [x] **P0-09 小程序 IM 未接入 WebSocket（已接入实时连接、断线重连与轮询降级）**
  - 代码：`miniprogram-client/services/im-socket.js:25`；`miniprogram-client/pages/im/room/index.js:439`。
  - 问题：页面实际使用 3 秒轮询，不是实时 WebSocket 链路。

- [x] **P0-10 WebSocket 语音文件未做归属校验（已与 HTTP 链路统一校验）**
  - 代码：`services/go-api/internal/appapi/im_ws.go:210-224`。
  - 问题：只校验 `image`、`file`，遗漏 `voice`；HTTP 链路与 WebSocket 链路规则不一致。

- [ ] **P0-11 SQL 仓储下 IM 归档不持久化**
  - 代码：`services/go-api/internal/im/service.go:858-872`。
  - 问题：`ArchiveRoomsByGameIDs` 只更新内存 map，没有保存数据库。

- [ ] **P0-12 局内消息通知未按房间聚合**
  - 代码：`services/go-api/internal/appapi/im_handler.go:296-320`。
  - 问题：每条消息都给每个成员新增一条通知，造成重复提醒和通知膨胀。

- [ ] **P0-13 组局驳回原因部分依赖进程内存**
  - 代码：`services/go-api/internal/appapi/game_handler.go:3974-3987`。
  - 问题：驳回原因额外写入 `gameAuditRejects` 内存 map，重启后丢失审核历史。

- [ ] **P0-14 手动开始没有填写并保存开始原因**
  - 代码：`services/go-api/internal/games/service.go:1351-1375`；`game_handler.go:4911-4928`。
  - 问题：接口没有 `startReason` 参数，也没有保存操作者、时间、原因。

## 二、高风险缺口（P1，12 项）

- [ ] **P1-01 角色申请资料文件未校验当前用户归属**
  - 代码：`services/go-api/internal/profiles/service.go:255-260`。
  - 问题：只校验文件 ID 数量和正数，没有校验上传者、业务类型和对象归属。

- [ ] **P1-02 角色申请等级条件与会员等级关闭口径冲突**
  - 代码：`services/go-api/internal/appapi/role_qualification.go:54-72`。
  - 问题：仍展示并校验行家 Lv.20、领路人 Lv.5。
  - 备注：需确认“关闭会员等级”是否仅关闭会员购买条件。

- [ ] **P1-03 并发审核缺少数据库事务/行锁**
  - 代码：`services/go-api/internal/games/service.go:1235-1320`；`games/sql_repository.go`。
  - 问题：主要依赖单进程 mutex，未发现事务、`FOR UPDATE` 或分布式锁。
  - 风险：多实例审核可能超员或计数不一致。

- [ ] **P1-04 我的局未区分参与、受邀和发起**
  - 代码：`miniprogram-client/pages/profile/service-center/my-games/index.js:188-196`。
  - 问题：没有加载受邀申请，发起局也可能混入我的局。

- [ ] **P1-05 局类型存在多套定义**
  - 代码：`services/go-api/internal/appapi/game_handler.go:432-470、490-513`；`admin-web/src/main.js:113-114`。
  - 问题：一级分类与免费/收费/AA/众筹/押金/条件局混用，前后台字段语义不统一。

- [ ] **P1-06 局状态机缺少锁定、争议、结算等完整状态**
  - 代码：`services/go-api/internal/games/service.go` 中当前状态分支。
  - 问题：未发现统一状态字典和状态迁移日志，文档要求的 `locked_started、disputed、settling、closed` 等状态没有完整落地。

- [ ] **P1-07 分润状态覆盖不完整**
  - 代码：`services/go-api/internal/revenue/service.go:383-590`；`db/migrations/000007_revenue_orders.sql`。
  - 问题：主要只有 `pending_settlement、frozen、settled`，缺少支付、争议、失败、回退、退款等状态。

- [ ] **P1-08 后台操作日志没有 before/after 快照**
  - 数据库：`db/migrations/000008_admin_system.sql`。
  - 问题：只有 `detail_json`，无法完整还原高风险修改前后的数据。

- [ ] **P1-09 每日任务、活动任务没有真实任务数据**
  - 代码：`services/go-api/internal/appapi/game_handler.go:1706-1750`；`miniprogram-client/pages/profile/task-center/index.wxml:36-38`。
  - 问题：两个分类始终返回空数组，仅显示占位文案。

- [ ] **P1-10 任务中心不是完整的可运营任务系统**
  - 代码：`services/go-api/internal/appapi/game_handler.go:1729-1735`。
  - 问题：新手任务按实时数据计算，未发现任务配置、领取、奖励流水和完成事件持久化。

- [ ] **P1-11 退出、扣信用、成员计数不是一个事务**
  - 代码：`services/go-api/internal/games/service.go:1943-1969`。
  - 问题：成员软退出、局人数更新、信用扣分流水分多次写入，异常时可能出现状态不一致。

- [ ] **P1-12 角色审核结果缺少明确站内通知链路**
  - 代码：`services/go-api/internal/profiles/service.go:373-430`。
  - 问题：看到状态更新和角色授予，但未看到审核通过/驳回通知创建逻辑，需要确认是否仅靠主动查询。

## 三、一期不应开放的二期能力（3 项）

- [ ] **P1-13 地图复杂页面仍注册开放**
  - 文件：`miniprogram-client/app.json`。
  - 页面包括 `blind-route、friend-city、my-city、footprint-heatmap、city-atlas` 等。

- [ ] **P1-14 地图默认配置仍包含路线盲盒、打卡挑战、好友分布、AR 等玩法**
  - 代码：`services/go-api/internal/appapi/map_handler.go:defaultMapIndexConfig`。

- [ ] **P1-15 二期地图接口仍可直接访问**
  - 代码：`services/go-api/internal/appapi/server.go:352-362`。
  - 接口包含路线、盲盒路线、打卡、挑战等，不符合一期只做定位、地点、距离、附近局的要求。

## 四、需要甲方确认的口径（3 项）

- [ ] **C-01 会员等级条件是否仅指会员购买等级**
  - 当前代码保留玩家成长等级 Lv.20/Lv.5，需要确认是否关闭。

- [ ] **C-02 一期是否允许好友文字私聊**
  - 文档要求好友私聊不做，但当前后端开放私聊接口；用户最新口径允许私聊保持文字为主。

- [ ] **C-03 四种局类型是一级分类还是收费/规则类型**
  - 当前代码同时存在任务、探索、成长、社交和免费、收费、AA、众筹等多套字段。

## 处理顺序建议

1. 先处理 P0-03、P0-04、P0-05：邀请、审核和成员权限。
2. 再处理 P0-06 至 P0-12：成团、IM、WebSocket、归档和通知。
3. 然后处理 P0-01、P0-02、P0-13、P0-14：角色资质、审核留痕和手动开局。
4. 甲方确认 C-01 至 C-03 后，再处理相关 P1 项。

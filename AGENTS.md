# AGENTS.md instructions for E:\项目\03周总\01enjoy

<INSTRUCTIONS>
技能文件、素材下载和外部资料固定放在 C:\Users\49637\Desktop\code-skill。
图标素材优先从 C:\Users\49637\Downloads\a\图标 查找；该目录是用户提供的外部图标素材目录，不是项目代码目录。需要用于小程序页面时，先复制到 E:\项目\03周总\01enjoy 内对应页面或组件的 assets 目录，再在代码中引用项目内路径。
项目代码文件固定放在 E:\项目\03周总\01enjoy。
修改小程序页面、样式、脚本、配置时，只编辑 E:\项目\03周总\01enjoy 内的项目代码。
不要把 C:\Users\49637\Desktop\code-skill 当作项目代码目录；它只作为技能文件和下载资料目录使用。
除非用户明确指定其他路径，原型截图、参考图、下载资料和临时外部资料都放到 C:\Users\49637\Desktop\code-skill。
若用户明确指定原型截图输出目录，例如 E:\项目\03周总\页面截图，则按用户指定目录保存，但不要把该目录当作项目代码目录。

单个对话上下文过长、跨越多个独立任务、包含大量截图/工具日志/代码修改，或已经开始影响判断时，应主动提醒用户新开对话。
提醒新开对话时，应先整理当前交接摘要：已完成内容、未完成事项、关键文件、关键规则、下一步建议；不要继续在超长对话中推进复杂修改。
若一个对话已经超过约 30-50 轮有效来回，或同一对话中混入 2-3 个以上独立主题，应优先建议新开对话继续。
切换会话或新开窗口继续旧任务时，不要假设新会话会自动完整继承旧上下文；应在旧会话先输出一段可直接粘贴到新会话的“交接开场白”。
交接开场白应包含：项目路径、当前目标、已完成内容、未完成事项、关键文件、重要规则、下一步建议、不要做的事。
新会话接到交接开场白后，应把它当作任务背景，但在继续修改前仍需重新检查当前文件状态、git 状态和相关代码，避免依赖过期上下文。
超过约 10-15 分钟的任务，或涉及多个文件/多个阶段的任务，应先任务化并给出停点：准备做什么、每一步完成后在哪里停、哪些动作需要用户确认。
任务执行中必须保持状态可见：开始前说明当前步骤；探索、修改、验证、遇到阻塞或发现范围变化时都要简短汇报；不要长时间静默推进。
每个阶段结束后应汇报：已完成什么、改了哪些文件、还剩什么、有没有风险或待确认事项；若用户不在线，也要把当前状态留在会话中，必要时补充到 docs/progress-日期.md 或 docs/follow-ups.md。
不得擅自扩大任务范围；如果发现需要从“资料整理”切到“代码实现”、从“静态 UI”切到“真实业务逻辑”、或需要新增接口/页面，应先说明并等待用户确认。

Git 保存 / 提交规则：
- 提交前必须先执行 `git status --short` 和针对本任务文件的 `git diff`，确认工作区是否混有其他任务或用户未提交改动。
- 禁止使用 `git add .`、`git add -A`、`git commit -a` 这类会把无关改动一起带入的命令。
- 只暂存本任务新增或修改的文件；若 `app.json`、`config/routes.js`、`project.private.config.json`、`docs/api-additions.md` 等文件同时混有其他任务改动，必须只暂存本任务相关片段，不能把其他页面入口、其他文档条目一起提交。
- 普通单任务文件可用 `git add -- path/to/file` 或 `git add -- path/to/dir` 暂存；混合文件优先使用精确片段暂存，例如 `git add -p path/to/file`。在非交互环境不适合 `git add -p` 时，可基于 `HEAD:path` 构造“只包含本任务改动”的内容，用 `git hash-object -w` 加 `git update-index --add --cacheinfo` 写入暂存区；这只写 Git 索引，不应覆盖工作区文件。
- 写入 Git 索引会创建 / 修改 `.git/index.lock` 和 `.git/index`，如果沙箱提示无权限，应按工具要求申请执行 `git add`、`git apply --cached`、`git hash-object`、`git update-index` 或 `git commit` 的权限，不要改用会污染工作区的绕路方法。
- 暂存后必须执行 `git diff --cached --name-status` 和 `git diff --cached --check`，并核对 staged diff 只包含本任务相关文件和片段。
- 提交时使用清晰业务信息，例如 `feat: add game invite page`；提交成功后回报提交号，并说明未提交的其它工作区改动仍保留。

邀请注册相关页面以墨刀原型为准，不要用旧截图、旧审计文档或自行推断的页面顺序替代墨刀当前画布。
墨刀地址：https://modao.cc/proto/BJBH1Uptgioo7R81pGx2/sharing?view_mode=read_only&screen=rbpVMU6bjYrmWMsnN
墨刀密码：vhni5m
旧墨刀入口 `screen=rbpVFDJKkOR1Pi9Cf` 与旧密码 `9o04o7` 已过期，只作为历史记录参考。
做邀请注册 5 页时，必须先从墨刀当前画布确认页面名称、顺序、尺寸、文案和点击关系，再修改小程序。
若用户要求“点一下看下一页”，只做墨刀 5 页静态走查，不接验证码、微信登录、注册等真实业务逻辑，也不要和其他页面流程联动。
墨刀密码不等于访问权限；若页面提示“链接无法访问，文件链接访问权限未开启”，不要继续猜测页面内容，也不要用旧资料替代，必须提示需要分享者开启链接访问权限、提供可访问链接，或改用已登录浏览器/导出的高清截图作为资料来源。
使用 z-mcp-modao 保存全部页面截图前，应确认墨刀链接在该工具使用的浏览器环境中可访问；若工具因浏览器残留进程、权限页或访问权限失败，应先说明原因，不要把失败截图当作原型页面。

项目里用户说“原型”时，默认代指上方墨刀原型里的当前内容。
设计页面上的元素尺寸、颜色、文案、间距、圆角、阴影和点击关系必须优先从墨刀原型读取；不能仅凭截图感觉或旧记录直接设计。
墨刀原型尺寸不能生搬硬套到小程序，需要按当前小程序画布/设备比例换算后再落到 rpx；换算前应先确认墨刀画布宽高和元素标注。
墨刀原型中 1 页就是设计实现中的 1 页；除非原型明确是可滚动页面，否则不要把单页画布做成上下滑动页面。
墨刀原型里的手机状态栏、微信胶囊、时间、电量、信号等系统环境元素不放进小程序页面里实现，除非用户明确要求画出来。
Stitch 或其他 AI 生成结果只能作为页面框架、结构和大致布局参考，不能作为元素精确尺寸、颜色、圆角、阴影、间距和点击关系的最终依据。
如果暂时只有 Stitch 框架而没有墨刀标注、可访问原型或高清截图，应先实现可替换的结构骨架，并把缺少精确 UI 资料的问题记录到 docs/follow-ups.md；不要把猜测尺寸写成最终还原。
落地 UI 时优先级为：当前可访问墨刀原型及其标注 > 当前导出的高清原型截图并配合画布尺寸换算 > 用户最新明确说明 > Stitch/AI 框架 > 旧截图或旧审计文档。
从原型换算到小程序时，应先确认原型画布宽高，再按小程序目标宽度换算 rpx；例如以 750rpx 为目标宽时，元素 rpx = 原型 px * 750 / 原型画布宽度。

代码命名必须使用业务语义或页面语义，不要使用设计工具来源命名。
墨刀只是设计来源，项目代码的页面、组件、样式类、变量和函数名不应使用 modao、modaoPage、modaoScreen、modaoStyle 等命名。
邀请注册相关代码建议使用 invite、invite-register、invite-flow、inviteSignup、realname、newbie-task 等业务命名。
modao 只适合出现在资料来源、文档记录、截图目录名或原型备注中，例如 docs/modao-*.md、prototype-source: modao；不要出现在实际业务代码命名里。

需要新增或待后端确认的接口，按日期记录到 docs/api-additions.md，写清模块、页面、功能、接口路径、用途、状态和记录日期。
需要后续完善的问题，按日期记录到 docs/follow-ups.md，写清模块、页面、功能、涉及接口或静态 UI、问题描述、来源和状态。
2026-06-14 重点后续完善：邀请注册 5 页静态走查中，第 3 页“实名认证弹窗”的盾牌图案仍需按墨刀继续修正；目标为紫粉渐变底、盾牌图案与墨刀一致，不要用不一致的自绘图案替代。
</INSTRUCTIONS>

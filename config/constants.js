const INVITE_STATUS_TEXT = {
  idle: '选填',
  valid: '已确认',
  invalid: '无效'
}

const INVITE_TIP = {
  idle: '没有邀请码也可以先完成微信授权登录',
  valid: '邀请码已记录，授权后会自动绑定',
  invalid: '请核对邀请码，或先完成微信授权登录'
}

module.exports = {
  INVITE_STATUS_TEXT,
  INVITE_TIP
}

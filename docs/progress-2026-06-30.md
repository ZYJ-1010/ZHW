# 2026-06-30 数据源清理进度

## 本轮已完成

- 全局数据源标志已从 `ENV.MOCK` 切到 `ENV.PROD`；请求层仍保留 mock 分支，但当前 `env.isMock` 为 `false`，页面不会从 mock 取数。
- `api/request.js` 补充 `DELETE` 请求封装，供白名单、屏蔽用户、关键词等删除接口使用。
- 个人中心 / 系统管理页批量清理页面 JS 静态业务数据，改为通过 `services/profile.js` 调真实接口：
  - 信用中心、成就墙。
  - 屏蔽设置：首页、保护模式、分场景配置、白名单、续期、关键词、用户屏蔽。
  - 建议反馈：首页、记录、详情。
  - 信用申诉。
  - 举报中心：首页、举报详情、处理记录、处理记录详情、申诉列表、申诉详情。
  - 签署协议：协议列表、协议详情、签署提交。
  - 系统设置：分组、行文案、右侧值、开关状态、清缓存、退出登录。
  - 我的资料：资料读取、保存；姓名、手机号、公司、职务、主营业务、资源和认证状态不再使用页面内样例。
- `api/modules/profile.js`、`services/profile.js` 已补充上述页面所需接口 wrapper。
- 组局模块已开始继续清理：
  - `pages/game/success-guide/index.js` 改为调用 `GET /api/app/game-invites/guide-success` 获取成局历程、参与双方、后续跟进入口。
  - `pages/game/guide-progress-detail/index.js` 去掉李娜、王强、中关村、固定倒计时、固定活动信息等本地详情兜底；接口为空时不再回填样例详情。
  - `api/modules/game.js`、`services/game.js` 已补 `getGuideSuccess`。
- `docs/api-additions.md` 已更新：
  - 成就墙、技能配置、屏蔽设置、建议反馈、信用申诉、举报中心、签署协议、系统设置、我的资料、组局成功领路人提示页等接口状态。
- `docs/follow-ups/profile.md` 已记录：
  - 生产数据源已切换，但生产域名仍待配置。
  - 签署协议、系统设置、我的资料等页面当前完成状态和待确认项。

## 已验证

- 已运行 `node --check` 覆盖本轮修改的 profile/system-management 相关 JS、`api/modules/profile.js`、`services/profile.js`、`api/request.js`、`config/env.js`。
- 已运行 `node --check` 覆盖 `api/modules/game.js`、`services/game.js`、`pages/game/success-guide/index.js`、`pages/game/guide-progress-detail/index.js`。
- 已用 `rg` 复查明显样例词：profile 侧只剩积分商城空 `DEFAULT_GOODS` 壳；系统设置、签署协议、我的资料的明显手机号 / 协议正文 / 默认用户样例已清掉。

## 尚未完成

- `config/env.js` 中 `ENV.PROD` 的 `baseUrl` 仍是占位 `https://api.example.com`，需要后端提供正式生产域名，并配置微信小程序 request 合法域名。
- 组局模块仍有页面 JS 静态业务数据待清理，优先顺序建议：
  - `pages/game/delivery/index.js`：服务交付详情、结算行、确认项、时间线仍是本地配置。
  - `pages/game/expert-cancel/index.js`：活动信息、赔付比例、取消原因、协议文案仍是本地数据。
  - `pages/game/apply/index.js`：在线人数、微信信息、自我介绍、留言等仍是本地默认值，提交未接 `applyGame`。
  - `pages/game/collaboration/index.js`、`pages/game/guide-chat/index.js`、`pages/game/greet/index.js`、`pages/game/hall-greet/index.js`：仍有本地联系人 / 消息样例或本地 `Date.now()` 消息 ID。
  - `pages/game/confirm/index.js`：上局确认接口失败仍有测试数据兜底。
  - `pages/game/payment/index.js`：仍有 `mockPayment` 文案和默认支付金额兜底需复核。
- 已有前序脏改动中仍包含 WXML/WXSS 和 mock 文件改动，本轮没有清理或回滚；后续提交需继续精确暂存，避免把无关 UI 改动混入。
- 上传文件、IM 会话、web-view 承载、部分“待接入”按钮动作仍需等后端接口或产品确认。

## 下一步建议

1. 先配置真实生产域名，确认生产接口能打通。
2. 继续从 `pages/game/delivery/index.js` 和 `pages/game/expert-cancel/index.js` 开始清理组局子页数据源。
3. 每处理一组页面后跑 `node --check` 和样例词 `rg`，再更新 `docs/api-additions.md` / follow-up。

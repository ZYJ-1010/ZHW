# 数据库设计清单（当前实现）

## 1. 基础设计

| 项目 | 内容 |
| --- | --- |
| 数据库 | PostgreSQL |
| 迁移文件 | 74 |
| 建表对象 | 103 |
| 显式索引 | 92 |
| 主键 | `bigserial`；关联表使用联合主键 |
| 时间 | `timestamptz` 为主；部分组局计划时间为 `varchar(32)` |
| 金额 | 资金域使用整数分；`games.price` 使用 `decimal(10,2)` |
| 扩展字段 | `jsonb` |
| 敏感数据 | 密文、掩码、哈希字段 |
| 删除 | 主要通过 `status`；未统一使用 `deleted_at` |
| 外键 | 少量显式外键；多数关系由应用层维护 |
| 迁移台账 | `schema_migrations` 按完整迁移文件名记录 |

## 2. 核心关系

| 主表 | 关联表 | 关联字段 |
| --- | --- | --- |
| `users` | `user_wechat_accounts` | `user_id` |
| `users` | `invite_codes` | `owner_user_id` |
| `invite_codes` | `invite_relations` | `invite_code_id` |
| `users` | `user_roles` | `user_id` |
| `users` | `role_applications` | `user_id` |
| `users` | `games` | `creator_user_id` |
| `games` | `game_locations` | `game_id` |
| `games` | `game_members` | `game_id` |
| `games` | `game_applications` | `game_id` |
| `games` | `game_invitations` | `game_id` |
| `games` | `chat_rooms` | `game_id` |
| `chat_rooms` | `chat_room_members` | `room_id` |
| `chat_rooms` | `chat_messages` | `room_id` |
| `games` | `reviews` | `game_id` |
| `users` | `user_growth_profiles` | `user_id` |
| `users` | `credit_accounts` | `user_id` |
| `users` | `points_accounts` | `user_id` |
| `games` | `revenue_records` | `game_id` |
| `revenue_records` | `revenue_record_items` | `revenue_record_id` |
| `users` | `reports` | `reporter_user_id`、`target_user_id` |
| `users` | `notifications` | `user_id` |

## 3. 表结构清单

### 000001_init_extensions.sql

#### `schema_migrations_marker`

- 字段：id、version、description、applied_at
- 后续变更：-
- 索引：0；唯一索引：0

### 000002_account_invite.sql

#### `invite_codes`

- 字段：id、code、owner_user_id、status、max_uses、used_count、expires_at、created_at、updated_at、entry_type
- 后续变更：000025_invite_entry_precheck.sql
- 索引：1；唯一索引：0

#### `invite_relations`

- 字段：id、invite_code_id、inviter_user_id、invitee_user_id、bind_source、created_at
- 后续变更：-
- 索引：3；唯一索引：0

#### `user_wechat_accounts`

- 字段：id、user_id、openid、unionid、session_key_encrypted、created_at、updated_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `users`

- 字段：id、status、nickname、avatar_url、mobile_encrypted、mobile_masked、realname_status、created_at、updated_at、avatar_file_id、mobile_hash、account_type
- 后续变更：000020_user_profiles_repository.sql、000036_user_phone_auth.sql、000065_platform_official_inviter.sql
- 索引：3；唯一索引：2

### 000003_realname_roles.sql

#### `guide_qualification_records`

- 字段：user_id、condition_met、payment_met、guide_open_status、updated_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `guide_qualification_rules`

- 字段：id、rule_code、min_invite_count、min_credit_score、min_completed_games、payment_required、status、updated_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `membership_plans`

- 字段：id、plan_code、plan_name、status、created_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `realname_auth_records`

- 字段：id、user_id、real_name_encrypted、id_card_encrypted、mobile_encrypted、status、reject_reason、created_at、updated_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `role_applications`

- 字段：id、user_id、role_code、status、reason、ability_description、proof_file_ids、reject_reason、review_admin_id、review_remark、created_at、updated_at、certificate_no、certified_at、eligibility_snapshot
- 后续变更：000038_role_application_certificates.sql、000058_role_application_eligibility_snapshot.sql
- 索引：2；唯一索引：2

#### `user_memberships`

- 字段：id、user_id、plan_code、status、started_at、expires_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `user_roles`

- 字段：id、user_id、role_code、status、created_at
- 后续变更：-
- 索引：0；唯一索引：0

### 000004_games.sql

#### `game_locations`

- 字段：id、game_id、longitude、latitude、address、poi_title、city_code、city_name、created_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `game_service_confirm_items`

- 字段：id、confirm_id、game_id、user_id、note、file_ids、created_at
- 后续变更：000004_games.sql
- 索引：1；唯一索引：0

#### `game_service_confirms`

- 字段：id、game_id、status、completed_at、created_at、updated_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `games`

- 字段：id、creator_user_id、title、game_type、game_source、status、min_players、max_players、current_players、city_code、city_name、created_at、updated_at、longitude、latitude、main_guide_user_id、primary_category、primary_category_text、secondary_category、secondary_category_text、type、address、cover_image、description、highlights、notice、audience、participation、price、profit_template、start_at、end_at、signup_start_at、signup_end_at、tags、completion_rules、allowed_roles、allow_guide_escort、distribution_method、payment_status、reject_reason、start_reason、started_by_user_id、started_at、introduction、description_media
- 后续变更：000016_games_repository.sql、000026_game_main_guide.sql、000028_game_categories.sql、000031_game_address.sql、000035_game_create_detail_fields.sql、000050_game_allowed_roles.sql、000051_game_guide_escort.sql、000052_phase2_commercial_reservations.sql、000053_game_rejection_reason.sql、000054_game_start_reason.sql、000070_game_introduction_and_description_media.sql
- 索引：6；唯一索引：0

### 000005_im.sql

#### `chat_messages`

- 字段：id、room_id、sender_user_id、client_msg_id、message_type、content、file_id、status、created_at
- 后续变更：-
- 索引：2；唯一索引：1

#### `chat_room_members`

- 字段：id、room_id、user_id、status、created_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `chat_rooms`

- 字段：id、game_id、status、created_at、engine、openim_group_id、archived_at、archive_reason
- 后续变更：000034_im_repository_fields.sql
- 索引：0；唯一索引：0

#### `content_risk_logs`

- 字段：id、game_id、room_id、message_id、sender_user_id、content、word、action、status、source、created_at
- 后续变更：-
- 索引：1；唯一索引：0

#### `sensitive_words`

- 字段：id、word、level、action、status、created_at
- 后续变更：-
- 索引：0；唯一索引：0

### 000006_review_growth_credit.sql

#### `achievements`

- 字段：id、code、title、created_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `credit_deduction_rules`

- 字段：id、rule_code、change_value、enabled、description、created_at、updated_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `credit_logs`

- 字段：id、user_id、game_id、change_value、before_score、after_score、reason、created_at、rule_code、source_type、source_id、role_snapshot、operator_type、operator_id、rule_version、appeal_id、idempotency_key
- 后续变更：000055_permanent_credit_accounts.sql
- 索引：1；唯一索引：1

#### `daily_credit_scores`

- 字段：id、user_id、score_date、current_score、created_at、updated_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `experience_logs`

- 字段：id、user_id、game_id、change_value、reason、created_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `points_accounts`

- 字段：id、user_id、available_points、frozen_points、total_earned_points、updated_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `points_logs`

- 字段：id、user_id、game_id、change_value、reason、created_at、before_points、after_points、biz_type、biz_id
- 后续变更：000017_points_redemption_repository.sql
- 索引：2；唯一索引：0

#### `review_reminders`

- 字段：id、game_id、user_id、status、deadline_at、created_at、reminder_type、notification_id、sent_at
- 后续变更：000006_review_growth_credit.sql
- 索引：2；唯一索引：0

#### `reviews`

- 字段：id、game_id、reviewer_user_id、target_user_id、target_role、score、content、again_intent、created_at、tags、nps_score
- 后续变更：000022_review_tags.sql、000049_review_nps_score.sql
- 索引：1；唯一索引：0

#### `user_achievements`

- 字段：id、user_id、achievement_code、achieved_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `user_footprints`

- 字段：id、user_id、game_id、action、created_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `user_growth_profiles`

- 字段：user_id、level、experience、review_count、updated_at
- 后续变更：-
- 索引：0；唯一索引：0

### 000007_revenue_orders.sql

#### `income_logs`

- 字段：id、user_id、revenue_record_id、change_value_cent、reason、created_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `payment_orders`

- 字段：id、order_no、user_id、game_id、amount_cent、pay_status、created_at、updated_at、order_type、business_type、provider_user_id、distribution_method、fulfillment_status、metadata
- 后续变更：000052_phase2_commercial_reservations.sql
- 索引：0；唯一索引：0

#### `revenue_record_items`

- 字段：id、revenue_record_id、user_id、role、amount_cent、created_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `revenue_records`

- 字段：id、revenue_record_no、game_id、template_id、status、total_amount_cent、frozen_reason、settled_at、created_at、updated_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `revenue_rules`

- 字段：id、template_id、rule_code、rule_value、created_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `revenue_templates`

- 字段：id、name、game_type、platform_bps、creator_bps、member_bps、status、created_at、updated_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `settlement_records`

- 字段：id、revenue_record_id、method、proof_no、amount_cent、created_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `user_income_accounts`

- 字段：id、user_id、total_cent、pending_cent、settled_cent、updated_at
- 后续变更：-
- 索引：0；唯一索引：0

### 000008_admin_system.sql

#### `admin_permissions`

- 字段：id、permission_code、permission_name、created_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `admin_role_permissions`

- 字段：role_id、permission_id、created_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `admin_roles`

- 字段：id、role_code、role_name、created_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `admin_user_roles`

- 字段：admin_user_id、role_id、created_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `admin_users`

- 字段：id、username、password_hash、status、created_at、updated_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `notifications`

- 字段：id、user_id、notify_type、title、content、biz_type、biz_id、status、need_wechat、wechat_state、wechat_template_id、wechat_task_id、created_at、read_at
- 后续变更：-
- 索引：2；唯一索引：0

#### `operation_logs`

- 字段：id、admin_user_id、action、target_type、target_id、request_id、ip、detail_json、created_at、before_json、after_json
- 后续变更：000055_operation_log_snapshots.sql
- 索引：2；唯一索引：0

#### `reports`

- 字段：id、game_id、reporter_user_id、target_user_id、report_type、content、status、chat_message_id、file_id、review_id、revenue_record_id、revenue_frozen、revenue_freeze_note、handler_admin_id、handle_result、handled_at、created_at、credit_log_id
- 后续变更：000032_report_credit_log_id.sql
- 索引：3；唯一索引：0

#### `wechat_subscribe_tasks`

- 字段：id、notification_id、user_id、scene、template_id、status、request_payload、result_code、result_message、created_at、sent_at
- 后续变更：-
- 索引：2；唯一索引：0

#### `wechat_subscribe_templates`

- 字段：id、scene、template_id、title、status、created_at
- 后续变更：-
- 索引：0；唯一索引：0

### 000009_behavior_export.sql

#### `export_tasks`

- 字段：id、task_no、template_code、export_type、status、file_id、created_by、filters_json、fail_reason、created_at、finished_at
- 后续变更：-
- 索引：2；唯一索引：0

#### `export_templates`

- 字段：id、code、name、export_type、file_name、columns_json、description、enabled、created_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `user_behavior_logs`

- 字段：id、user_id、event_type、target_type、target_id、page_path、keyword、extra、created_at、event_code、business_type、business_id、source、device、ip、occurred_at
- 后续变更：000013_behavior_log_fields.sql
- 索引：5；唯一索引：0

### 000010_p1_reserved.sql

#### `connection_follow_logs`

- 字段：id、connection_id、operator_user_id、follow_type、content、next_follow_at、created_at
- 后续变更：-
- 索引：1；唯一索引：0

#### `delivery_documents`

- 字段：id、doc_type、title、status、created_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `expert_skill_profiles`

- 字段：id、user_id、skill_tree、service_tags、case_file_ids、updated_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `game_checkins`

- 字段：id、game_id、user_id、milestone_id、checkin_type、content、file_ids、status、created_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `game_continue_drafts`

- 字段：id、original_game_id、draft_game_id、creator_user_id、title、status、created_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `game_favorites`

- 字段：id、user_id、game_id、created_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `game_milestones`

- 字段：id、game_id、title、status、created_at、updated_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `game_retrospectives`

- 字段：id、game_id、user_id、content、again_intent、created_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `guide_resource_profiles`

- 字段：id、user_id、resource_tags、industry_tags、city_codes、connection_scale、updated_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `member_report_snapshots`

- 字段：id、user_id、report_type、period、metrics、created_at
- 后续变更：-
- 索引：1；唯一索引：0

#### `redemption_items`

- 字段：id、name、description、points_cost、stock、status、created_at、updated_at
- 后续变更：-
- 索引：1；唯一索引：0

#### `redemption_orders`

- 字段：id、order_no、user_id、item_id、points_cost、status、review_admin_id、review_reason、created_at、updated_at
- 后续变更：-
- 索引：2；唯一索引：0

#### `team_relations`

- 字段：id、leader_user_id、member_user_id、relation_level、source、status、created_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `test_cases`

- 字段：id、module、case_name、priority、expected_result、created_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `test_runs`

- 字段：id、case_id、result、actual_result、request_id、created_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `user_connections`

- 字段：id、user_id、connected_user_id、relation_type、source_type、source_id、strength_score、created_at、updated_at
- 后续变更：-
- 索引：1；唯一索引：0

### 000011_identity_strong.sql

#### `app_auth_sessions`

- 字段：id、token_hash、user_id、session_kind、expires_at、revoked_at、created_at
- 后续变更：-
- 索引：1；唯一索引：0

#### `identity_faceid_sessions`

- 字段：id、user_id、face_token_hash、status、detect_auth_payload_digest、callback_payload_digest、failure_reason、created_at、completed_at
- 后续变更：-
- 索引：1；唯一索引：0

#### `identity_sms_code_records`

- 字段：id、user_id、scene、phone_masked、code_hash、sent_at、expires_at、verified_at、verify_failed_count、created_at
- 后续变更：-
- 索引：1；唯一索引：0

#### `identity_verification_records`

- 字段：id、user_id、status、phone_masked、phone_encrypted、phone_hash、sms_verified、phone_verified、face_verified、wechat_realname_consistency、failure_reason、last_sms_sent_at、sms_daily_key、sms_daily_count、last_face_token_hash、faceid_request_id、faceid_result_digest、created_at、updated_at、real_name_masked、id_card_masked、real_name_ciphertext、real_name_initials、id_card_ciphertext
- 后续变更：000023_identity_masked_fields.sql、000039_identity_in_game_identity.sql、000051_identity_id_card_ciphertext.sql
- 索引：1；唯一索引：0

### 000012_ai_data_prepare.sql

#### `ai_data_export_tasks`

- 字段：id、export_type、status、blocked_reason、created_by_admin_id、created_at、completed_at
- 后续变更：-
- 索引：1；唯一索引：0

#### `ai_data_snapshots`

- 字段：id、behavior_log_count、favorite_count、review_count、connection_count、expert_profile_count、guide_profile_count、im_message_count、data_ready、im_export_enabled、created_at
- 后续变更：-
- 索引：0；唯一索引：0

### 000014_payment_callback_placeholder.sql

#### `payment_callbacks`

- 字段：id、provider、callback_type、event_id、order_no、out_trade_no、transaction_id、raw_headers_json、raw_body、payload_digest、verify_status、process_status、error_message、created_at、processed_at
- 后续变更：-
- 索引：4；唯一索引：1

### 000015_files_repository.sql

#### `files`

- 字段：id、uploader_user_id、biz_type、object_id、file_name、mime_type、size_bytes、sha256、storage_key、access_level、expires_at、created_at
- 后续变更：-
- 索引：3；唯一索引：1

### 000016_games_repository.sql

#### `game_applications`

- 字段：id、game_id、user_id、status、reason、created_at、reviewed_at、file_ids、role、reject_reason
- 后续变更：000030_game_application_file_ids.sql、000037_game_application_invitation_roles.sql、000052_game_application_rejection_reason.sql
- 索引：3；唯一索引：1

#### `game_members`

- 字段：game_id、user_id、role、status、quit_reason、credit_deducted、credit_log_id、joined_at
- 后续变更：-
- 索引：1；唯一索引：0

### 000018_user_location_records.sql

#### `user_location_records`

- 字段：id、user_id、longitude、latitude、accuracy_meter、accuracy_warning、city_code、city_name、address、source、created_at
- 后续变更：-
- 索引：2；唯一索引：0

### 000019_team_member_repository.sql

#### `member_report_memberships`

- 字段：user_id、plan_name、invited_count、created_at、updated_at
- 后续变更：-
- 索引：1；唯一索引：0

#### `teams`

- 字段：id、leader_user_id、name、status、created_at
- 后续变更：-
- 索引：1；唯一索引：0

### 000020_user_profiles_repository.sql

#### `user_profiles`

- 字段：id、user_id、gender、bio、interest_tags、city_code、city_name、created_at、updated_at
- 后续变更：-
- 索引：1；唯一索引：0

### 000024_game_invitations.sql

#### `game_invitations`

- 字段：id、game_id、inviter_user_id、target_user_id、status、message、application_id、created_at、responded_at、role、service_type、service_duration_text、demand_detail、budget_amount_cent、expected_time_text、invite_group_id、player_user_id、expert_user_id
- 后续变更：000037_game_application_invitation_roles.sql、000046_game_invitation_detail_fields.sql、000047_game_invitation_groups.sql、000048_game_invitation_parties.sql
- 索引：5；唯一索引：0

### 000029_system_configs.sql

#### `system_configs`

- 字段：id、config_key、config_value、status、created_at、updated_at
- 后续变更：-
- 索引：1；唯一索引：0

### 000033_user_system_management_configs.sql

#### `user_system_management_configs`

- 字段：user_id、config_key、config_value、updated_at
- 后续变更：-
- 索引：1；唯一索引：0

### 000050_private_chat.sql

#### `private_chat_conversations`

- 字段：id、user_a_id、user_b_id、source_game_id、status、created_at、updated_at
- 后续变更：-
- 索引：0；唯一索引：0

#### `private_chat_messages`

- 字段：id、conversation_id、sender_user_id、target_user_id、source_game_id、message_type、content、status、created_at
- 后续变更：-
- 索引：2；唯一索引：0

### 000052_phase2_commercial_reservations.sql

#### `commercial_order_allocations`

- 字段：id、commercial_order_id、recipient_user_id、role、amount_cent、allocation_status、created_at、updated_at
- 后续变更：-
- 索引：1；唯一索引：0

#### `commercial_service_orders`

- 字段：id、order_no、order_type、game_id、buyer_user_id、provider_user_id、amount_cent、payment_status、fulfillment_status、distribution_method、platform_fee_bps、metadata、created_at、updated_at
- 后续变更：-
- 索引：2；唯一索引：0

### 000053_enterprise_certifications.sql

#### `enterprise_certifications`

- 字段：id、user_id、company_name、unified_social_credit_code、legal_person、business_license_file_id、public_account_file_id、status、reject_reason、review_admin_id、review_remark、created_at、updated_at
- 后续变更：-
- 索引：2；唯一索引：1

### 000055_permanent_credit_accounts.sql

#### `credit_accounts`

- 字段：user_id、current_score、updated_at
- 后续变更：-
- 索引：1；唯一索引：0

### 000056_role_growth_event_ledger.sql

#### `growth_event_ledger`

- 字段：id、user_id、game_id、role_code、event_code、idempotency_key、occurred_at、created_at
- 后续变更：-
- 索引：2；唯一索引：0

### 000056_user_task_progress.sql

#### `user_task_progress`

- 字段：user_id、task_code、completed_at
- 后续变更：-
- 索引：1；唯一索引：0

### 000057_game_status_logs.sql

#### `game_status_logs`

- 字段：id、game_id、from_status、to_status、operator_user_id、reason、created_at
- 后续变更：-
- 索引：1；唯一索引：0

### 000059_export_file_contents.sql

#### `export_file_contents`

- 字段：file_id、content、created_at
- 后续变更：-
- 索引：0；唯一索引：0

### 000060_game_create_drafts.sql

#### `game_create_drafts`

- 字段：id、creator_user_id、title、payload、created_at、updated_at
- 后续变更：-
- 索引：1；唯一索引：0

### 000062_invite_code_operations.sql

#### `invite_quota_requests`

- 字段：id、owner_user_id、quantity、reason、status、audit_reason、reviewed_by、created_at、reviewed_at
- 后续变更：-
- 索引：2；唯一索引：0

### 000063_user_login_passwords.sql

#### `user_login_passwords`

- 字段：user_id、password_hash、updated_at
- 后续变更：-
- 索引：0；唯一索引：0

### 000067_user_newbie_guide_progress.sql

#### `user_newbie_guide_progress`

- 字段：user_id、profile_reminder_count、last_profile_reminder_at、updated_at
- 后续变更：-
- 索引：0；唯一索引：0

### 000068_user_login_activity.sql

#### `user_login_activity`

- 字段：user_id、last_app_login_at、updated_at
- 后续变更：-
- 索引：1；唯一索引：0

### 000069_game_progress_feedbacks.sql

#### `game_progress_feedbacks`

- 字段：id、game_id、user_id、progress、content、file_ids、created_at
- 后续变更：-
- 索引：1；唯一索引：0

## 4. 数据约束清单

| 对象 | 约束 |
| --- | --- |
| 微信账号 | `openid` 唯一 |
| 邀请码 | `code` 唯一 |
| 用户角色 | `user_id + role_code` 唯一 |
| 待审核角色申请 | 同一用户同一角色仅允许一条 `pending` |
| 局成员 | `game_id + user_id` 主键 |
| 入局申请 | 同一用户同一局仅允许一条待审核申请 |
| 局邀约 | 按局、目标用户和状态控制重复 |
| 群聊客户端消息 | `room_id + sender_user_id + client_msg_id` 唯一 |
| 评价 | `game_id + reviewer + target + target_role` 唯一 |
| 信用流水 | 用户和幂等键唯一 |
| 成长事件 | `idempotency_key` 唯一 |
| 分润模板 | 三方基点合计 10000 |
| 分润记录 | 每个局一条记录 |
| 私聊会话 | 双方用户唯一且不能相同 |
| 企业认证 | 同一用户仅允许一条待审核记录 |

## 5. 当前问题清单

| 优先级 | 问题 |
| --- | --- |
| P0 | 多数 `user_id`、`game_id`、`file_id` 没有外键 |
| P0 | 组局计划时间部分使用字符串 |
| P0 | 元和分两套金额单位并存 |
| P0 | 实名、位置、信用存在新旧表并存 |
| P1 | 大量状态字段为自由字符串 |
| P1 | `JSONB` 配置缺少统一版本 Schema |
| P1 | 消息、行为、通知、日志未设计分区和归档 |
| P1 | 敏感数据需补密钥轮换、访问审计和保留期限 |

## 6. 迁移文件

| 用途 | 文件 |
| --- | --- |
| 新库初始化 | `deploy/postgres/init-prod.sh` |
| 增量迁移 | `db/migrate-prod.sh` |
| 迁移目录 | `db/migrations/*.sql` |
| 种子目录 | `db/seeds/*.sql` |
| 字段机器清单 | `docs/数据库表清单-20260729.csv` |

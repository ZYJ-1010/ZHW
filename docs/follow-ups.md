# 后续完善记录

本文记录需要后续继续完善的页面、功能、接口和静态 UI 问题。新对话继续开发前，应先查看本文和项目根目录 `AGENTS.md`。

## 后续模块索引

历史记录暂保留在本文，不做迁移。自 2026-06-24 起，新增待补充事项优先按模块写入 `docs/follow-ups/` 下的模块文档，本文只补充模块索引，避免多个任务频繁改同一个大表导致 Git 冲突。

| 模块 | 文档 |
| --- | --- |
| 组局 / 组局详情 | [`docs/follow-ups/game-detail.md`](follow-ups/game-detail.md) |
| 组局 / 组局大厅 | [`docs/follow-ups/game-hall.md`](follow-ups/game-hall.md) |
| 组局 / 发起组局 | [`docs/follow-ups/game-create.md`](follow-ups/game-create.md) |
| 地图 / 城市探索 | [`docs/follow-ups/map.md`](follow-ups/map.md) |
| 地图 / 足迹热力图 | [`docs/follow-ups/footprint-heatmap.md`](follow-ups/footprint-heatmap.md) |
| 消息 / 消息页 | [`docs/follow-ups/message.md`](follow-ups/message.md) |

## 2026-06-14

| 模块 | 页面 | 功能 / 区域 | 类型 | 问题 | 来源 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 邀请注册 | 邀请注册 5 页静态走查 - 第 3 页 `实名认证弹窗` | 盾牌图案 | 静态 UI | 当前盾牌图案仍需按墨刀修正。目标是紫粉渐变底，盾牌图形、描边、红色斜杠和底色比例与墨刀一致；不要使用不一致的自绘替代。 | 用户 2026-06-14 反馈 | 待完善 |
| 邀请注册 / 新手任务 | `pages/login/index` 第 5 页 `完成新手任务` | 完善个人资料任务 | 页面补充 | 当前 `profile` 类型任务先跳 `pages/profile/index`；后续需要补充正式个人资料完善页或编辑入口。 | 用户 2026-06-14 反馈 | 待完善 |
| 邀请注册 / 新手任务 | `pages/login/index` 第 5 页 `完成新手任务` | 发布第一个局任务 | 页面补充 | 当前 `first_game` 类型任务先跳 `pages/game/create/index`；后续需要按发布第一个局流程补充完善。 | 用户 2026-06-14 反馈 | 待完善 |
| 邀请注册 / 新手任务 | `pages/login/index` 第 5 页 `完成新手任务` | 进入首页按钮 | 页面补充 | 当前 `进入首页` 先跳 `pages/home/index`；后续如需改为个人首页，再按正式入口调整。 | 用户 2026-06-14 反馈 | 待完善 |

## 2026-06-16

| 模块 | 页面 | 功能 / 区域 | 类型 | 问题 | 来源 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 首页 / 角色页面走查 | `pages/home/index` 新增 HTML 转小程序页 | 墨刀对比 | 原型访问 | 当前有效墨刀入口为 `screen=rbpVMU6bjYrmWMsnN`、密码 `vhni5m`，但 z-mcp 墨刀页面枚举工具在当前浏览器环境中超时，尚未完成页面树 / 截图对比。后续需使用可访问的已登录浏览器环境或用户导出的高清原型截图继续核对页面名称、顺序、尺寸、文案和点击关系。 | 用户 2026-06-16 更新原型链接与密码；工具核验超时 | 待核对 |

| 邀请注册 / 实名认证 | `pages/login/realname/index` | 测试默认实名信息 | 测试辅助 | 当前测试阶段默认填入测试姓名和测试身份证号，方便走查通过；上线或联调真实实名接口前必须删除默认值。 | 用户 2026-06-16 反馈 | 待删除 |

## 2026-06-17

| 模块 | 页面 | 功能 / 区域 | 类型 | 问题 | 来源 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 首页 / 玩家首页 | `pages/home/player/index`、`components/game-card/index.*` | 附近正在发生 / 朋友都在玩卡片头像 | 素材 / 接口 | 用户提供的 HTML 目录和对外资料包中未找到截图里的三枚卡通头像原图；当前公共卡片已支持 `avatarUrls`，测试阶段先用默认占位，正式阶段需要后台返回玩家头像 URL 或补齐设计头像素材。 | 用户 2026-06-17 指定资料路径并截图确认 | 待后端 / 素材确认 |

## 2026-06-21

| 模块 | 页面 | 功能 / 区域 | 类型 | 问题 | 来源 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 组局大厅 | `pages/game/hall/index` | 分类入口圆形按钮内部图标 | 静态 UI | 已按当前导出页还原分类文案、圆形渐变底和大致图标语义；导出包中图标为嵌套 FRAME / 矢量路径，尚未逐个确认相机、多人、奖杯、公文包、九宫格的精确路径和比例。后续如需像素级一致，应从墨刀标注或可直接导出的 SVG 图标继续替换。 | 用户 2026-06-21 指定墨刀导出目录和截图 | 待核对 |
| 组局大厅 | `pages/game/hall/index` | 搜索框、筛选条、卡片渐变 / 阴影 | 静态 UI | 已按导出画布可解出的尺寸、文案和颜色近似落地；部分导出填充编码和阴影编码未完全反解，具体渐变停靠点、阴影透明度仍需墨刀标注模式或高清截图继续确认。 | 用户 2026-06-21 指定墨刀导出目录和截图 | 待核对 |

## 2026-06-22

| 模块 | 页面 | 功能 / 区域 | 类型 | 问题 | 来源 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 组局 / 发起邀请 | `pages/game/invite/index` 第 2 步 `选择玩家` | 玩家添加数量上限 | 产品 / 接口 | 当前先按最多添加 1 位玩家实现，页面进入时从邀请玩家配置接口读取 `minPlayerCount=1`、`maxPlayerCount=1` 并展示 `已添加 X/1 位玩家`；后续最多可添加几个玩家仍需用户 / 产品确认，确认后由后台配置返回并决定是否改为多选。 | 用户 2026-06-22 反馈 | 待确认 |
| 组局 / 领路人组局进度查询 | `pages/game/guide-progress/index` | 进行中组局卡片 | 产品 / 静态 UI | 当前进度查询页按 1 个玩家对 1 个行家展示，卡片左侧为玩家、右侧为行家，进行中数量进入页面时从后台获取。若后续出现多玩家或多行家，需要甲方补充多人 / 多行家的卡片展示方式、成员数量露出规则、确认状态聚合规则和详情入口表现。 | 用户 2026-06-22 反馈 | 待甲方补充 |
| 组局 / 领路人查阅进度详情 | `pages/game/guide-progress-detail/index` | 当前状态卡倒计时与进度条 | 组件 / 业务规则 | 当前先复用 `components/game-detail/status-card` 的 `progress` 变体承载“当前状态、超时倒计时、进度条”静态展示；后续需补充倒计时计算规则后替换为会自动变化的倒计时逻辑，且下方进度条必须按总时长与剩余时间比例同步递减，不能使用随意或固定比例。 | 用户 2026-06-22 反馈 | 待规则补充 |
| 组局 / 领路人查阅进度详情 | `pages/game/guide-progress-detail/index` | 提醒行家 | 交互 / 文案 | 当前“提醒行家 / 再次提醒”先保留待接入提示；后续需确认按钮行为是进入发消息页、预填消息、直接发送提醒，还是调用提醒接口，提醒消息内容也待产品确认。 | 用户 2026-06-22 反馈 | 待确认 |
| 组局 / 领路人查阅进度详情 | `pages/game/guide-progress-detail/index` | 取消组局 | 页面补充 / 交互 | 当前底部“取消组局”先保留待接入提示；后续需确认是否新增取消组局页，并补充取消原因、确认取消、接口调用和取消后状态流转。 | 用户 2026-06-22 反馈 | 待确认 |
| 组局 / 成功行家提示 | `pages/game/success-expert/index` | 静态设计页精确还原 | 静态 UI | 当前按用户提供的 HTML 设计稿落地“组局成功”空白母版页面；若后续需要像素级还原，仍需补充当前墨刀标注或高清原型截图继续核对尺寸、色值、圆角、阴影和点击关系。 | 用户 2026-06-22 提供 HTML 设计稿 | 待核对 |
| 组局 / 成功行家提示 | `pages/game/success-expert/index` 点击 `进入群聊` | 局内群聊入口 | 页面补充 / 参数传递 | 后续点击 `进入群聊` 应进入该组局的群聊界面，并把当前点击者在此组局中的角色一并传入，例如行家、领路人或玩家；群聊具体界面和真实 IM 逻辑后续再开发。 | 用户 2026-06-22 反馈 | 待开发 |
| 组局 / 成功行家提示 | `pages/game/success-expert/index` | 活动信息 / 资金状态 / 三方群数据 | 接口 / 字段核对 | 当前页面活动信息、资金状态、三方群和下一步仍为静态占位；正式开发需与后台核对成功态详情接口、字段命名、资金状态枚举、金额单位、时间格式和当前用户组局角色。详见 `docs/api-additions.md` 第 26 节。 | 用户 2026-06-22 反馈 | 待后端确认 |
| 组局 / 成功行家提示 | `pages/game/success-expert/index` | 静态改数据驱动方案 | 实现方式 | 后续不要直接继续把活动信息、资金状态、下一步状态写死；应保留当前设计稿结构，新增 `successDetail` 数据对象和 `normalizeSuccessDetail(data)`，由接口返回控制 `group`、`viewer.role`、`activityRows`、`fund`、`nextSteps`。暂不抽组件，等页面尺寸稳定或其他页面复用资金状态 / 下一步卡时再抽组件。设计参数继续按用户给的 pt 坐标换算为 rpx，默认 `1pt = 2rpx`，先改对应页面样式类，不擅自改业务逻辑。 | 用户 2026-06-22 反馈 | 待实现 |
| 组局 / 成功行家提示 | `pages/game/success-expert/index` 下一步 `联系玩家确认具体时间` | 玩家聊天入口 | 页面补充 / 参数传递 | 后续点击第一步“联系玩家确认具体时间”应跳转到与玩家的一对一聊天框，并携带 `gameId`、玩家 ID、当前用户在组局中的角色等参数；当前仅静态展示，未实现点击跳转和聊天页。 | 用户 2026-06-22 反馈 | 待开发 |
| 组局 / 成功领路人提示 | `pages/game/success-guide/index` 后续跟进 `查看组局日程` | 查看局日程 | 接口 / 页面补充 | 后续点击“查看组局日程”应查看该局日程；具体是跳转局详情 / 日程页、当前页弹层，还是调用独立日程接口待定。详见 `docs/api-additions.md` 第 27 节。 | 用户 2026-06-22 反馈 | 待后端 / 产品确认 |
| 组局 / 成功领路人提示 | `pages/game/success-guide/index` 后续跟进 `询问双方反馈` | 联系玩家和行家咨询反馈 | 交互 / 接口 | 后续点击“询问双方反馈”应弹出联系框，用于和玩家、行家聊天咨询反馈；联系框样式、双方会话入口、默认话术和反馈回传接口待定。详见 `docs/api-additions.md` 第 27 节。 | 用户 2026-06-22 反馈 | 待后端 / 产品确认 |
| 组局 / 成功领路人提示 | `pages/game/success-guide/index` 后续跟进 `促成交易` | 达成合作交易 | 业务 / 接口 | 后续点击“促成交易”用于推进或记录双方达成合作交易；是否创建交易 / 合作记录、是否进入表单或订单页、接口字段和状态流转待定。详见 `docs/api-additions.md` 第 27 节。 | 用户 2026-06-22 反馈 | 待后端 / 产品确认 |
| 组局 / 我的业务管理 - 专家 | `pages/game/manage/index` 进行中服务卡片 | 管理操作 4 个按钮 | 交互 / 接口 | 当前先不实现真实行为，仅记录待补充：`提前结束交付` 点击后应弹出确认交付按钮 / 确认弹窗，确认后调用接口改变服务状态；`取消并赔付` 点击后应弹出专家赔付取消界面，展示赔付规则、金额和确认取消；`联系玩家` 应进入或创建与玩家的消息会话并给玩家发消息；`联系领路人` 应进入或创建与领路人的消息会话并给领路人发消息。正式实现需从后台返回当前专家角色的服务信息、玩家 ID、领路人 ID、订单 / 服务 ID 和可执行按钮权限。详见 `docs/api-additions.md` 第 28 节。 | 用户 2026-06-22 反馈 | 待实现 / 待后端确认 |
| 组局 / 取消服务确认 - 专家 | `pages/game/expert-cancel/index` | 合同金额 / 赔付金额 / 平台手续费 / 实际扣款 | 接口 / 数据来源 | 当前页面已支持从页面参数读取合同金额，并兼容 `platformFeeRate`、`platformFee`、`platformFeeText` 作为平台手续费预览；正式联调时合同金额、赔付金额、平台手续费和实际扣款必须由后端预览接口返回或以后端规则计算，前端展示值不能作为正式结算依据。详见 `docs/api-additions.md` 第 28 节。 | 用户 2026-06-23 反馈 | 待后端确认 |
| 组局 / 取消服务确认 - 专家 | `pages/game/expert-cancel/index` | 确认取消并赔付 | 接口 / 交互 | 当前确认按钮只做取消原因、详细说明和协议勾选的前端校验，真实取消赔付提交接口、提交成功后的状态刷新、返回路径、支付或托管资金扣除结果处理均未接入；后续需补充专家取消赔付提交接口。详见 `docs/api-additions.md` 第 28 节。 | 用户 2026-06-23 反馈 | 待后端确认 / 待实现 |
| 组局 / 我的业务管理 - 专家 | `pages/game/manage/index` 已完成卡片 | 评价双方按钮样式与交互 | 静态 UI / 交互 / 接口 | 已完成服务卡片下方需要 `评价双方` 按钮，未评价时可点击，已评价后置灰禁用；评价状态必须由后台返回。按钮最终宽度、图标、颜色、禁用样式、点击后进入评价页还是弹出评价框，以及评价完成后如何刷新状态，仍需甲方 / 产品确认后细化。 | 用户 2026-06-22 反馈 | 待甲方确认 / 待实现 |
| 组局 / 我的业务管理 - 专家 | `pages/game/manage/index` 已完成 / 争议列表 | 取消服务与争议服务卡片样式 | 静态 UI / 产品确认 | 已完成列表里后续可能包含“已取消”的服务活动，卡片需要展示取消原因；争议 tab 也需要对应服务卡片样式。取消服务与争议服务的最终卡片样式、归属规则、字段展示顺序、状态胶囊、取消原因展示、赔付金额 / 退款金额是否展示，以及色彩口径暂由甲方后续确定，当前先不实现，不再按现有截图擅自落地灰色取消卡。 | 用户 2026-06-22 反馈 | 待甲方确认 |
| 组局 / 我的业务管理 - 专家 | `pages/game/manage/index` 顶部统计与状态 tab | 数量和选中状态对应关系 | 产品 / 接口 | 顶部收入卡统计展示 `进行中 / 待结算 / 已完成`，下方筛选 tab 展示 `进行中 / 已完成 / 争议`；两组数量和状态归属需要甲方确认：`待结算` 是否属于已完成 tab、取消服务是否属于已完成还是争议、默认进入页面选中哪个 tab，以及 tab 数量是否完全由后台返回。当前先不继续改实现，待确认后再调整。 | 用户 2026-06-22 反馈 | 待甲方确认 |

## 2026-06-23

| 模块 | 页面 | 功能 / 区域 | 类型 | 问题 | 来源 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 组局 / 我的引荐记录 - 领路人 | `pages/game/referral-record/index` | 收益统计、tab 数量、服务记录列表 | 接口 / 数据驱动 | 当前页面为静态走查占位；正式实现时收益统计、玩家信息、行家信息、引荐编号、服务标题、引荐奖励、预计交付时间、取消原因、评价状态、tab 数量都必须由后台返回。tab 需要筛选三类服务：`进行中 / 已完成 / 已取消`，不能前端写死最终业务数据。详见 `docs/api-additions.md` 第 29 节。 | 用户 2026-06-23 反馈 | 待后端确认 |
| 组局 / 我的引荐记录 - 领路人 | `pages/game/referral-record/index` 进行中服务卡片 | `提醒交付`、`查看群聊` | 接口 / 交互 | 当前按钮只保留待接入提示；后续 `提醒交付` 需要确认是直接发提醒还是进入聊天页预填话术，`查看群聊` 需要确认使用已有群聊 `conversationId` 还是调用接口创建 / 获取会话，并携带组局 ID、引荐 ID、玩家 ID、行家 ID 和当前领路人角色。详见 `docs/api-additions.md` 第 29 节。 | 用户 2026-06-23 反馈 | 待后端确认 / 待实现 |
| 组局 / 服务交付 - 有偿 / 免费 | `pages/game/delivery/index` | 快捷操作 `联系玩家`、`联系领路人`、确认状态 `提醒确认`、底部 `确认服务完成` | 交互 / 接口 | 当前页面先做静态展示和待接入提示，不实现真实跳转或提交。后续 `联系玩家` 应进入或创建与玩家的消息会话并给玩家发消息；`联系领路人` 应进入或创建与领路人的消息会话并给领路人发消息；确认状态里的 `提醒确认` 应给玩家发消息 / 提醒玩家进行服务完成确认；底部 `确认服务完成` 应提交服务完成确认，并触发玩家确认、结算状态刷新或免费局归档流程。详见 `docs/api-additions.md` 第 34 节。 | 用户 2026-06-23 反馈 | 待实现 / 待后端确认 |
| 组局 / 组局管理 - 玩家 | `pages/game/player-manage/index` 进行中服务卡片 | 服务数据与进度计算 | 接口 / 数据驱动 | 编号、资金金额、专家、领路人、预计交付时间、剩余天数都必须由后台返回；前端根据 `startedAt`、`expectedDeliveryAt` 和 `currentTime/serverTime` 计算进度百分比和进度条。详见 `docs/api-additions.md` 第 30 节。 | 用户 2026-06-23 反馈 | 待后端确认 |
| 组局 / 组局管理 - 玩家 | `pages/game/player-manage/index` 进行中服务卡片 | `联系行家`、`申请取消` | 交互 / 接口 | 当前按钮先保留待接入提示；后续 `联系行家` 点击后应进入或创建与行家的消息会话并给行家发消息，`申请取消` 点击后应弹出取消窗口，展示赔付规则、取消原因和确认取消操作。详见 `docs/api-additions.md` 第 30 节。 | 用户 2026-06-23 反馈 | 待实现 / 待后端确认 |
| 组局 / 组局管理 - 玩家 | `pages/game/player-manage/index` 已完成服务卡片 | 评价双方 | 交互 / 接口 | 已完成服务下方展示 `评价双方` 按钮；是否已评价由后台 `reviewStatus/reviewed/canReview` 等字段确定。未评价时可点击，已评价后置灰禁用。评价页和真实提交逻辑后续再接入。详见 `docs/api-additions.md` 第 30 节。 | 用户 2026-06-23 反馈 | 待实现 / 待后端确认 |
| 全局 / 活动信息头像 | `components/service-activity-card` 及使用文字头像的组局页面 | 头像缩写 | 静态 UI / 统一规则 | 圆形文字头像必须按项目统一规则显示姓名姓氏拼音前两个字母，例如 `李明` 显示 `LI`、`张专家` 显示 `ZH`；不能显示中文截断，也不要在单个页面里擅自改成不同规则。若后台返回 `avatarText`，也应符合该规则或由前端统一规范后展示。 | 用户 2026-06-23 反馈 | 统一规则 / 已记录 |
| 组局 / 取消申请 - 玩家 | `pages/game/player-cancel/index` | 确认取消并赔付 | 接口 / 交互 | 当前按用户提供的 `申请取消服务.txt` 设计稿完成白色母版静态走查；合同金额、已服务时长和活动总时长必须在进入页面前由上一页订单数据或取消预览接口给出，服务时长展示为 `已服务时长 / 活动总时长`，页面只展示和消费这些数据，静态兜底仅用于走查；`可协商` 标签需要放在 `自定义赔付比例` 标题行右侧，后续点击进入与行家 / 专家的聊天界面沟通赔付比例，当前先不实现真实跳转；建议赔付比例暂定 `15%`，最低 `5%`、最高 `30%` 仅为当前静态口径，正式联调时最低比例、最高比例、默认建议比例和比例调整后的金额重算应由后台接口支持；赔付预览、金额计算权威来源、取消原因枚举、确认提交、微信支付或托管资金扣除、取消后状态刷新仍需后端 / 产品确认。详见 `docs/api-additions.md` 第 32 节。 | 用户 2026-06-23 反馈 | 待后端确认 / 待实现 |
| 组局 / 组局取消 - 领路人 | `pages/game/guide-cancel/index` | 推荐他人 / 寻找其他玩家 | 页面补充 | `重新组局` 点击直接跳转 `pages/game/create/index`；`推荐他人` 后续应进入“寻找其他玩家”页面，当前先不实现新页面和真实筛选逻辑，仅保留待接入提示。 | 用户 2026-06-23 反馈 | 待实现 |
| 组局 / 局内协作 | `pages/game/collaboration/index` | 进度情况 / 成员 / 聊天区 / 底部操作 | 接口 / 交互 | 当前页面已按白色母版 + 深色内容区静态落地，进度情况、成员、聊天区 3 个区域都需要保持圆角卡片。正式实现时进度情况、成员、聊天区消息均从后台获取；`成员管理` 点击后应列出当前局成员；`结束本局` 点击后应跳转结束确认页面。详见 `docs/api-additions.md` 第 33 节。 | 用户 2026-06-23 反馈 | 待后端确认 / 待实现 |
| 组局 / 服务评价 - 行家 | `pages/game/review/index` | 提交评价 | 接口 / 跳转 | 当前页面只完成静态表单和前端选中态；点击 `提交评价` 暂时提示待接入。后续需要把页面里的满意度、故事文本、玩家评价、领路人评价、标签、NPS、当前用户角色、组局 ID / 服务订单 ID / 引荐 ID 等发送给后台；提交成功后跳转到哪里也需要产品确认，可能返回服务交付页、我的业务管理、服务详情或成功提示页。详见 `docs/api-additions.md` 第 35 节。 | 用户 2026-06-23 反馈 | 待后端 / 产品确认 |
| 组局 / 评价完成 | `pages/game/review-complete/index` | 再玩意愿 3 个按钮 | 交互 / 跳转 | 当前先保留静态选中态；后续需要实现三个按钮行为：`再玩一局` 点击后向下跳到再玩一局推荐区域 / 后续再玩流程，`暂停` 点击后返回组局大厅，`不玩了` 点击后也返回组局大厅。具体是否调用记录意愿接口、是否携带组局 ID / 服务订单 ID、是否先提交意愿再跳转，待产品和后端确认。 | 用户 2026-06-23 反馈 | 待实现 / 待确认 |

## 2026-06-24

| 模块 | 页面 | 功能 / 区域 | 类型 | 问题 | 来源 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 组局 / 再玩一局推荐 | `pages/game/play-again/index` | 关闭按钮、同局好友再玩一局、系统推荐适配组局、玩家创建新局 | 交互 / 跳转 / 接口待确认 | 当前页面只完成静态弹层、选项高亮和待接入提示；后续需要补齐真实行为：关闭按钮应关闭弹层并返回上一上下文或评价完成页；`同局好友再玩一局` 应基于上一局参与人发起再次邀请或进入邀请确认流程；`系统推荐适配组局` 应进入推荐组局列表或调用匹配推荐接口；`玩家创建新局` 应跳转创建组局并按当前上下文预填可复用信息。是否需要记录用户选择、携带 `gameId` / `serviceOrderId` / 上一局成员 ID，以及接口路径和状态流转，仍需产品和后端确认。 | 用户 2026-06-24 反馈 | 待实现 / 待确认 |
| 组局 / 组局确认 | `pages/game/confirm/index` | 再次组局发起人和邀请对象 | 产品 / 接口待确认 | 当前测试默认“领路人发起，再邀请上一局里的行家和玩家”，页面只展示这两方并在确认后进入组局进度流程。正式规则需要甲方确认：是否固定领路人发起；如果玩家或行家也能发起，是否只邀请另外两方；非参与方是否允许基于上一局三方再次组局。接口字段详见 `docs/api-additions.md` 第 36 节。 | 用户 2026-06-24 反馈 | 待甲方 / 后端确认 |
| 组局 / 系统推荐适配局 | `pages/game/system-recommend/index` | 推荐数据、确认组局跳转 | 接口 / 页面补充 | 当前页面已改为从 `GET /api/app/game-invites/system-recommendations` 读取推荐上下文、筛选标签和行家列表；点击 `确认组局` 先跳 `pages/game/create/index` 并携带 `source=systemRecommend`、推荐 ID、已选行家 ID、上一局 / 服务订单上下文。后续需要确认正式推荐接口、确认后是否直接创建组局，以及 `pages/game/create/index` 如何接收并预填这些参数。详见 `docs/api-additions.md` 第 37 节。 | 用户 2026-06-24 反馈 | 待后端 / 产品确认 |

## 2026-06-25

| 模块 | 页面 | 功能 / 区域 | 类型 | 问题 | 来源 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 地图 / 盲盒路线 | `pages/map/blind-route/index` | 路线卡片点击 / 完成 | 交互 / 后续实现 | 当前页面先完成静态展示、卡片选中态和最近开启区域；路线卡片点击后的正式行为后续再实现，需要确认是直接开启盲盒路线、进入路线详情、发起组局，还是完成后回写路线状态，并同步刷新最近开启列表。 | 用户 2026-06-25 反馈 | 待实现 / 待确认 |

## 记录规则

- 新增项必须写清：日期、模块、页面、功能 / 区域、类型、问题、来源、状态。
- 不要因为普通 UI 小修改、已经当场解决的反馈、临时走查结果而新增 follow-up；只有真正跨会话未解决、需要产品 / 后端确认，或用户明确要求记录时才写。
- 历史记录暂不迁移；后续新增待补充事项优先拆到对应页面 / 模块文档，做哪个页面就查哪个页面相关文档。`docs/follow-ups.md` 只维护索引和记录规则，减少 Git 冲突。
- 只有新增模块文档时才更新本文索引；模块内新增具体事项时，不要反复改本文。
- 如果涉及接口，补充接口路径、请求方式、字段、是否已有后端文档，并同步记录到 `docs/api-additions.md`。
- 如果只涉及静态 UI，注明“静态 UI”，并写清对应墨刀页面或用户截图来源。

# 组局 / 发起组局后续完善

本文记录 `pages/game/create/index` 的后续完善事项。新增项必须写清日期、模块、页面、功能 / 区域、类型、问题、来源、状态；涉及接口时同步补充 `docs/api-additions.md`。

## 2026-06-24

| 模块 | 页面 | 功能 / 区域 | 类型 | 问题 | 来源 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 组局 / 发起组局 | `pages/game/create/index` | 局封面图片命名规则 | 产品 / 校验规则待确认 | 当前页面已增加封面图片本地选择、替换、删除和 JPG / PNG 等格式校验；封面图片命名规则尚未明确，仅记录为后续注意事项，页面不展示。后续需确认是否按局 ID、用户 ID、时间戳、业务前缀或后端返回规则命名。 | 用户 2026-06-24 反馈 | 待产品 / 后端确认 |
| 组局 / 发起组局 | `pages/game/create/index` | 局主题字数限制 | 产品规则 | 当前前端按用户反馈限制局主题最多 20 个字，并在输入框下方显示 `当前字数/20`。后续如产品确认不同上限，应同步调整前端 `THEME_MAX_LENGTH` 和后端校验规则。 | 用户 2026-06-24 反馈 | 已按当前口径实现 / 待产品最终确认 |
| 组局 / 发起组局 | `pages/game/create/index` | 局人数总人数限制 | 产品 / 接口校验规则 | 当前前端按用户反馈限制局人数总人数最少 3 人、最多 10 人，滑块、输入框归一化和发布按钮可用性校验均使用该范围。后续发布 / 保存草稿接口也必须按 `3 <= capacity <= 10` 强校验，避免绕过前端提交不合法人数。 | 用户 2026-06-24 反馈 | 前端已实现 / 待后端接口强校验 |
| 组局 / 发起组局 | `pages/game/create/index` | 局介绍字数限制 | 产品 / 校验规则 | 当前前端按用户反馈限制局介绍最多 200 字，并在输入区下方显示 `当前字数/200`。局介绍提示文案作为可见说明展示，支持自然换行。后续保存草稿 / 发布接口应同步按 `intro.length <= 200` 校验。 | 用户 2026-06-24 反馈 | 前端已实现 / 待后端接口强校验 |
| 组局 / 发起组局 | `pages/game/create/index` | 亮点字数限制 | 产品 / 校验规则 | 当前前端按用户反馈限制亮点最多 100 字，并在输入区下方显示 `当前字数/100`。亮点说明文案作为可见说明展示在输入框外。后续保存草稿 / 发布接口应同步按 `highlights.length <= 100` 校验。 | 用户 2026-06-24 反馈 | 前端已实现 / 待后端接口强校验 |
| 组局 / 发起组局 | `pages/game/create/index` | 活动须知字数限制 | 产品 / 校验规则 | 当前前端按用户反馈限制活动须知最多 100 字，并在输入框下方显示 `当前字数/100`。活动须知说明文案作为可见说明展示在输入框外。后续保存草稿 / 发布接口应同步按 `notice.length <= 100` 校验。 | 用户 2026-06-24 反馈 | 前端已实现 / 待后端接口强校验 |
| 组局 / 发起组局 | `pages/game/create/index` | 活动受众字数限制 | 产品 / 校验规则 | 当前前端按用户反馈限制活动受众最多 50 字，并在输入框下方显示 `当前字数/50`。活动受众说明文案作为可见说明展示在输入框外。后续保存草稿 / 发布接口应同步按 `audience.length <= 50` 校验。 | 用户 2026-06-24 反馈 | 前端已实现 / 待后端接口强校验 |
| 组局 / 发起组局 | `pages/game/create/index` | 分润模板后台配置 | 接口 / 配置待确认 | 当前前端已改为从后台获取分润模板列表，并在接口失败或未返回模板时使用默认模板兜底。后续需确认 `GET /api/app/games/profit-templates` 是否为正式接口、模板字段、默认选中逻辑、是否按 `feeType` 和账号类型过滤。 | 用户 2026-06-24 反馈 | 前端已接入 mock / 待后端确认 |
| 组局 / 发起组局 | `pages/game/create/index` | 公益局发起权限 | 权限 / 接口强校验 | 公益局只能平台账户发起，普通用户不可选，玩家、行家、领路人都不能发起。当前前端会将非平台账户看到的公益局置灰并阻止选择；后端仍必须在模板下发、保存草稿和发布创建组局接口强校验，禁止绕过前端提交公益局。 | 用户 2026-06-24 反馈 | 前端已防御 / 待后端强校验 |
| 组局 / 发起组局 | `pages/game/create/index` | 押金局规则与提示 | 接口 / 配置待确认 | 押金局规则和押金局提示应由后台配置下发，前端未收到时使用当前默认文案：规则为“连续打卡 7 天即完成。完成者拿回押金池金额，未完成者押金由完成者平分。”；提示为“支付金额：100元 = 服务费10元 + 押金池90元。服务费不退，押金池按完成情况结算。” | 用户 2026-06-24 反馈 | 前端已接入 mock / 待后端确认 |
| 组局 / 发起组局 | `pages/game/create/index` | 底部操作按钮 | 功能 / 页面待实现 | `保存草稿` 后续保存到草稿箱；`浏览提交` 后续弹出预览页面，把用户填写和选择的内容按字段展示，底部提供提交按钮；`发布并同步到地球网` 后续接入正式发布同步能力。当前只记录功能口径，不在本轮实现草稿箱、预览提交页或地球网发布同步。 | 用户 2026-06-24 反馈 | 待实现 |

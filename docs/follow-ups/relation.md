# 关系模块后续完善

本文记录关系模块后续需要补齐的页面、接口和静态 UI 问题。

## 2026-06-25

| 模块 | 页面 | 功能 / 区域 | 类型 | 问题 | 来源 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 关系 / 关系网首页 | `pages/relation/network/index` | 顶部浮层数据 | 接口 / 数据驱动 | 当前已新增抽象层接口 `GET /api/app/relations/network-home` 和 mock 数据，页面可从服务层读取地点标题、营业状态、地址、tab 和在线人数；正式接口路径、字段、地点来源和刷新策略仍需后端确认。 | 用户 2026-06-25 反馈“这里要抽象层接口” | 待后端确认 |
| 关系 / 关系网首页 | `pages/relation/network/index` | 关系网地图主体 | 静态 UI / 后续实现 | 当前主体关系网图先使用用户提供模板里的静态图片资产 `relation-network-map.png` 作为走查视觉；后续如需真实关系网交互，需要改为数据驱动节点、连线、附近玩家和点击详情，并确认节点字段、坐标算法和缩放 / 拖拽规则。 | 用户 2026-06-25 指定本页先这样 | 待实现 |
| 关系 / 关系网首页 | `pages/relation/network/index` | tab 行为 | 交互 / 后续实现 | 当前 `人脉网络 / 附近玩家` 只切换选中态；后续需要确认是否切换不同数据层、是否刷新关系网图、是否接附近玩家列表或地图点位接口。 | 用户 2026-06-25 指定本页先这样 | 待确认 |

# 消息模块待完善事项

## 2026-06-26

| 模块 | 页面 | 功能 / 区域 | 类型 | 问题 | 来源 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 消息 / 交易预警 | `pages/message/trade-warning/index` | `申请延期`、`立即交付` | 交互 / 接口 | 当前按钮只保留待接入提示，未调用真实提交接口。后续需要确认申请延期表单、延期原因、立即交付提交参数、提交成功后的订单状态刷新和跳转路径。 | 用户 2026-06-26 要求记录未做内容 | 待后端 / 产品确认 |
| 消息 / 交易预警 | `pages/message/trade-warning/index` | 交付方式 | 交互 / 接口 | 当前只实现 `线上确认 / 上传凭证` 的本地选中态，未接上传文件、凭证预览、删除凭证、线上确认提交等真实流程。 | 用户 2026-06-26 要求记录未做内容 | 待实现 |
| 消息 / 交易预警 | `pages/message/trade-warning/index` | 剩余交付时间 | 接口 / 业务规则 | 当前页面从交易预警详情接口读取展示用倒计时数组，没有实现基于 `serverTime` 和 `expectedDeliveryAt` 的实时倒计时。后续需确认倒计时由后端直接返回展示值，还是前端按服务端时间计算。 | 用户 2026-06-26 要求记录未做内容 | 待后端确认 |
| 消息 / 交易预警 | `pages/message/trade-warning/index` | 预警详情接口 | 接口 / 字段核对 | 当前候选接口为 `GET /api/app/messages/trade-warning`，字段已记录到 `docs/api-additions.md` 第 46 节；正式路径、入参和字段命名仍需与后端确认。 | 用户 2026-06-26 要求记录未做内容 | 待后端确认 |
| 消息 / 系统通知 | `pages/message/system-detail/index` | 文章详情格式 | 接口 / 字段核对 | 当前页面按后台返回的块结构渲染文章内容和统计数据，候选接口为 `GET /api/app/messages/system-notification`，字段已记录到 `docs/api-additions.md` 第 47 节；正式接口路径、富文本还是块结构、图片域名和字段命名仍需后端确认。 | 用户 2026-06-26 反馈文章由后台推送 | 待后端确认 |
| 消息 / 系统通知 | `pages/message/system-detail/index` | 有用 / 没用反馈 | 交互 / 接口 | 当前有用 / 没用统计从详情接口读取，点击按钮仍只保留待接入提示；后续若需要回写统计，需要补充反馈提交接口、重复点击规则和提交后的统计刷新策略。 | 用户 2026-06-26 反馈统计来自后台 | 待实现 / 待后端确认 |

# 我的 / 个人中心后续完善

## 2026-06-26

| 页面 | 功能 / 区域 | 类型 | 问题 | 来源 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `pages/profile/index` | 个人中心首页 | 静态 UI / 接口 / 交互 | 当前已按用户粘贴的“个人中心 - 真好玩”HTML 替换原个人中心页，并套用 `母版-申请加入` 对应的 `home-shell` `joinApply` 变体；页面隐藏母版标题和右侧头像，菜单图标使用项目内 `pages/profile/assets/i66@3x.png` 至 `i85@3x.png`。用户信息、资产、服务中心、资产中心、足迹中心、账户管理均为静态走查数据，未接真实个人中心聚合接口。 | 用户 2026-06-26 粘贴 HTML 并要求代替原个人中心页 | 待后端确认 / 待实现 |
| `pages/profile/index` | 首页入口跳转 | 跳转 / 页面补充 | 当前已接入的跳转：`我的局` -> `pages/profile/service-center/my-games/index`，`我的邀请` -> `pages/profile/service-center/invite/overview/index`，`评价管理` -> `pages/profile/service-center/manage/review-manage/index`，`我的资产` / `查看全部资产` -> `pages/profile/asset-center/manage/index`，`积分商城` -> `pages/profile/asset-center/mall/index`，`我的积分` -> `pages/profile/asset-center/points/index`，`我的成就墙` -> `pages/profile/footprint/achievements/index`，`我的资料` -> `pages/profile/system-management/profile-info/index`，`技能配置` -> `pages/profile/system-management/skill-config/index`，`屏蔽设置` -> `pages/profile/system-management/block-settings/index`，`建议反馈` -> `pages/profile/system-management/feedback/index`，`举报中心` -> `pages/profile/system-management/report-center/index`，`信用中心` -> `pages/profile/credit-center/index`，`系统设置` -> `pages/profile/settings/index`，`增购会员` -> `pages/profile/member/index`。`组局管理` 仅作为服务中心下的目录层级，不再保留独立页面或首页入口。仍需补齐或确认入口：我的押金、开票中心、我的足迹、我的城市故事、签署协议。 | 用户 2026-06-26 要求记录页面跳转和信息接口；用户 2026-06-27 追加资产中心相关设计页；用户 2026-06-27 提供“资料设置”HTML 并要求新增“我的-系统管理-我的资料”编译模式；用户 2026-06-27 提供“技能配置”HTML 并要求白色母版和编译模式；用户 2026-06-28 提供屏蔽设置参考资料并要求新增页面和编译模式；用户 2026-06-28 提供建议反馈资料目录并要求新增页面和编译模式；用户 2026-06-28 提供举报中心资料目录并要求新增页面和编译模式 | 待确认 / 待实现 |
| `pages/profile/asset-center/manage/index` | 资产中心 - 资产管理 | 静态 UI / 接口 / 交互 | 当前已按用户粘贴的“资产管理”HTML 设计页落地白色母版静态走查，展示总资产、总成交额、可提现、待结算、提现 / 充值、余额明细、银行卡、订单状态、最近订单和常见问题；个人中心“我的资产”和资产卡“查看全部”已进入本页。当前所有展示数据都只是静态兜底，后续必须由后台提供：资产金额汇总、可提现 / 待结算金额、银行卡绑定数量与银行卡列表、余额明细、订单状态统计以及各状态对应的订单列表、最近订单、FAQ 文案和操作入口可用状态。提现、充值、银行卡管理、订单筛选 / 查看全部、最近订单点击仍未接真实接口。 | 用户 2026-06-27 粘贴 HTML 并要求白色母版设计页；用户 2026-06-27 要求记录数据由后台提供 | 待后端确认 / 待实现 |
| `pages/profile/asset-center/manage/index` | 资产中心 - 资产管理子入口 | 页面补充 / 路由 / 接口 | 当前资产管理页的“我的订单”已进入 `pages/profile/asset-center/orders/index` 静态走查页。仍缺少子页面：余额明细、银行卡管理、订单状态筛选结果页、提现页、充值页、FAQ 详情或帮助页。后续需要根据产品原型确认是否新建在 `pages/profile/asset-center/` 下，以及每个入口的路由、列表字段、分页、空态、错误态和真实接口。 | 用户 2026-06-27 指出缺少余额明细等页面；用户 2026-06-27 提供积分商城 / 我的订单参考图 | 待补页面 / 待后端确认 |
| `pages/profile/asset-center/mall/index`、`pages/profile/asset-center/orders/index` | 资产中心 - 积分商城 / 我的订单 | 静态 UI / 接口 / 交互 | 当前积分商城和我的订单已按用户参考图落地深色静态走查，并新增编译模式“我的-资产中心-积分商城”“我的-资产中心-我的订单”；价格行奖杯使用金橙渐变底，商品图标保留当前本地页面实现。积分商城已接前端 `GET /api/app/profile/points/mall` 与 `POST /api/app/profile/points/mall/exchange` 服务 / mock 骨架：点击商品在当前页弹确认兑换框，确认后按后台返回弹成功 / 失败提示，并重新查询商城刷新积分余额和库存。我的订单页已改为通过 `GET /api/app/profile/points/orders` 按当前状态 tab 查询后台 / mock 数据，状态切换不再只改本地高亮；订单动作中 `查看物流` 已改为在我的订单当前页打开物流弹层，并按 `GET /api/app/profile/points/orders/{orderId}/logistics` 读取快递公司、运单号和物流轨迹；`再次兑换` 已跳转积分商城；`查看详情` 和 `取消订单` 都应跳转独立页面但页面形态、路由和字段仍需业主确认，当前先保留待接入提示。正式联调仍需确认订单字段、状态枚举、分页、空态、兑换订单同步刷新、物流弹层字段、订单详情页、取消订单页和取消结果接口。 | 用户 2026-06-27 提供参考图并补充奖杯底样式；用户 2026-06-27 要求兑换后按后台结果提示并刷新库存；用户 2026-06-27 要求订单状态切换按钮生效且订单数据来自后台；用户 2026-06-27 说明取消订单和查看详情需记录待业主确认页面，再次兑换跳转商城；用户 2026-06-27 要求物流详情改为当前页弹层展示，并确认删除单独物流详情页 | 待业主确认 / 待后端联调 |
| `pages/profile/asset-center/mall/index` | 积分商城 - 确认兑换弹框 | 接口 / 交互 / 状态 | 当前确认兑换弹框已在当前页展示，按钮已做兑换中状态、防重复点击、按后台返回成功 / 失败弹提示框、并重新查询商城刷新库存。后续仍需完善：确认正式兑换接口路径、字段名、失败码和提示文案；确认后台是否在同一事务内完成积分扣减、库存锁定、订单生成和幂等处理；确认兑换成功后的订单状态是否统一进入 `待发货`，以及是否需要立即刷新积分明细和我的订单列表；确认库存不足、积分不足、商品下架、重复提交、网络超时等异常场景的返回结构；确认是否需要展示收货地址、兑换规则、发货时效、售后 / 取消规则和订单详情入口。 | 用户 2026-06-27 要求记录兑换接口、按后台结果提示、刷新库存，并追问确认购买后订单锁定逻辑 | 待后端确认 / 待联调 |
| `pages/profile/asset-center/points/index` | 资产中心 - 我的积分 / 积分中心 | 静态 UI / 接口 / 交互 | 当前已按用户粘贴的“积分中心”HTML 设计页落地静态走查，展示可用积分、累计积分、已兑换、过期积分、积分规则、赚积分、角色分润积分示例、积分明细筛选和底部说明；个人中心“我的积分”已进入本页。页面展示数据、积分规则配置、积分比例、计算规则、各角色分成比例、角色分润积分示例、积分明细筛选、兑换记录、积分商城跳转和过期处理均应由后台提供，前端只负责展示后台返回内容，不计算或推导分润比例和积分结果，当前仅保留静态兜底。 | 用户 2026-06-27 粘贴 HTML 并要求同样做出我的积分页；用户 2026-06-27 说明积分比例、计算规则、角色分成和积分明细均由后台提供 | 待后端确认 / 待实现 |
| `pages/profile/index` | 个人中心首页信息接口 | 接口 / 数据驱动 | 首页当前静态数据包括用户昵称、会员等级、成长等级、角色、头像、引荐数、成功数、成交总额、信用度、资产三项、VIP 提示、各分组菜单、角标状态和入口路由。后续需要由 `GET /api/app/profile/home` 或正式个人中心聚合接口返回，前端不再写死展示数据、角标和入口可见性。 | 用户 2026-06-26 要求记录页面跳转和信息接口 | 待后端确认 |
| `pages/profile/service-center/manage/review-manage/index`、`pages/profile/service-center/manage/review-reply/index` | 服务中心 / 组局管理 - 评价管理与回复评价 | 静态 UI / 接口 / 交互 | 当前两页已套用 `home-apply-shell`（母版-申请页），标题分别为“评价管理”和“回复评价”，并按用户反馈调整为：评价管理页点击“立即回复 / 回复评价”进入回复评价页，点赞为图标按钮；回复评价页 200 字限制、快捷回复模板为小标签、历史对话显示玩家 / 行家头像，提交成功后暂按本地回写上一页“行家回复”内容。未完成内容：评价管理页评分统计、待回复数量、评价列表、筛选、查看订单、平台介入状态、点赞 / 取消点赞仍未接真实接口；回复评价页评价详情、快捷回复模板、历史对话仍使用本地兜底数据，未按 `reviewId` 从后台获取；提交回复已接前端服务和 Mock，正式联调时需以后端返回的回复内容、回复时间和评价状态刷新列表，不依赖本地回写。需后端获取 / 确认的数据：评价列表与统计、单条评价详情、快捷回复模板、历史对话、提交回复结果、点赞状态与点赞数量。 | 用户 2026-06-26 提供 HTML 并持续反馈样式和交互 | 待后端确认 / 待实现 |
| `pages/profile/match-info/index` | 适配信息表单 | 静态 UI / 接口 / 交互 | 当前按导出文案展示字段和保存按钮，未接个人主页填写、选择器、地址定位、保存接口和表单校验。 | 用户 2026-06-26 指定个人中心资料目录 | 待后端确认 / 待实现 |
| `pages/profile/service-center/my-games/index` | 服务中心 - 我的局列表、筛选、评价、再次购买 | 静态 UI / 接口 / 交互 | 当前已迁到服务中心子页路径，并按用户截图改为空白母版、标题“我的局”、顶部筛选图标、三段关系 tab、状态筛选和订单卡片静态走查；进行中卡片保留小钟表“预计交付：03-25 14:00”和右侧下拉箭头。未接我的局列表、筛选弹层、评价、再次购买、联系会话和详情展开流程。 | 用户 2026-06-26 截图反馈并要求放到服务中心子页 | 待后端确认 / 待实现 |
| `pages/profile/settings/index` | 系统管理 - 系统设置 | 静态 UI / 接口 / 交互 | 当前已按 `E:\项目\03周总\系统管理\08系统设置` 下新提供的系统设置 HTML 和两张参考图替换旧系统设置页，并新增“我的-系统管理-系统设置 / 静态页 / 动态页”编译模式；页面套用 `profile-white-shell` 白色母版导航，内容区按参考图深色卡片、regular 字重、账号安全 / 通知设置 / 隐私设置 / 通用设置和退出登录按钮还原。图标来自参考资料中的本地图标雪碧图，已裁剪并复制到 `pages/profile/settings/assets` 后引用；未使用 CSS 自绘图标或文字 emoji。当前开关只做本地静态切换，支付密码、登录密码、更换手机号、免打扰、隐私清单、清除缓存、关于我们和退出登录均未接真实业务。 | 用户 2026-06-28 提供系统管理 08 系统设置资料目录并要求删除旧页面、按新代码和图片还原、应用白色母版、编译模式按“我的-系统管理-系统设置”命名；用户追问图标是否有 emoji，确认本页按截图线性图标还原 | 待后端确认 / 待运行截图复核 |
| `pages/profile/credit-center/index` | 信用中心 | 静态 UI / 接口 | 当前按导出文案展示信用分、等级、奖励、惩罚和明细，未接信用分、信用等级、奖励领取、处罚记录和信用明细接口。 | 用户 2026-06-26 指定个人中心资料目录 | 待后端确认 / 待实现 |
| `pages/profile/service-center/invite-records/index` | 服务中心 - 我的邀请、超时预警、提醒交付、查看详情 | 静态 UI / 接口 / 交互 | 当前已从 `pages/profile/invite-records/index` 迁到服务中心层级，标题改为“我的邀请”；未接我引荐的 / 我发起的列表、状态筛选、提醒交付、详情页和超时处理逻辑。 | 用户 2026-06-26 指定服务中心目录树 | 待后端确认 / 待实现 |
| `pages/profile/service-center/invite/overview/index`、`pages/profile/service-center/invite/network/index`、`pages/profile/service-center/invite/records/index`、`pages/profile/service-center/invite/ranking/index`、`pages/profile/service-center/invite/income/index`、`pages/profile/service-center/invite/member-detail/index` | 服务中心 / 我的邀请 - 服务中心首页、关系网络、邀约记录、贡献排行、收益明细、成员详情 | 静态 UI / 接口 / 交互 | 当前已按 `E:\项目\03周总\服务中心` 下 6 个导出文件夹新增静态走查页，并按用户最新截图统一调整为深色邀请中心风格；`pages/profile/service-center/invite/overview/index` 是服务中心“我的邀请”首页，顶部展示“数据概览、关系网络、邀约记录、贡献排行、收益明细”标签，个人中心入口和 `我的-服务中心-我的邀请` 编译模式均指向该页。当前页面数据均为本地静态兜底，未接数据概览、关系网络、邀约记录、贡献排行、收益明细、成员详情接口；顶部快捷操作按“分享邀请码 / 二维码 / 生成海报”处理，中间项不再使用复制链接口径，需要从后台获取邀请码、二维码 / 小程序码、带邀请码海报图；关系网络页下方一级成员明细点击进入成员详情页并携带 `memberId`，后续成员详情需按该 id 从后台获取资料；查看分润、提醒交付、查看详情、排行切换和筛选切换均未接真实交互。 | 用户 2026-06-27 指定服务中心 6 个文件夹和编译模式，并提供深色样式截图 | 待后端确认 / 待实现 |
| `pages/profile/service-center/invite/records/index` | 服务中心 / 我的邀请 - 邀约记录页 | 静态 UI / 接口 / 交互 | 当前页面只做静态走查展示，前端显示的数据后续都应由后台返回，包括顶部 `我引荐的 / 我发起的` 数量与选中态、状态筛选数量、超时预警文案、卡片状态、记录编号、时间、行家 / 玩家头像昵称与角色、关系节点、服务标题、预算、你的奖励、已完成卡 `已到账`、超时 / 取消提示、右侧查看组局入口是否展示。未完成：真实列表接口、筛选联动、超时预警点击后的处理、查看组局跳转对应详情、提醒交付、状态流转、分页 / 空态 / 错误态、字段和状态枚举确认；前端不写死最终业务数据。 | 用户 2026-06-27 要求记录当前未完成和后台获取内容 | 待后端确认 / 待实现 |
| `pages/profile/service-center/invite/ranking/index` | 服务中心 / 我的邀请 - 贡献排行页 | 静态 UI / 接口 / 交互 | 当前已按用户提供截图调整为深色榜单页，标题、周期标签、排行切换、榜单姓名和数值均按 regular 字体处理；页面数据暂为本地静态兜底。正式联调时周期筛选、本周 / 本月 / 本季 / 本年 / 全部、邀约数排行 / 分润贡献排行、成员排名、头像、昵称、等级、活跃天数、邀约数、分润贡献、分页 / 空态 / 错误态都应由后台返回，前端不写死最终业务数据。 | 用户 2026-06-27 提供贡献排行截图并说明数据来自后台 | 待后端确认 / 待实现 |
| `pages/profile/service-center/invite/income/index` | 服务中心 / 我的邀请 - 收益明细页 | 静态 UI / 接口 / 交互 | 当前已按深色收益明细原型调整静态 UI，标题、区块标题、指标卡和流水文字均按 regular 处理；收益趋势不再写死 10 个月，当前由本地 `trendPoints` 示例数据循环渲染，正式联调时后台返回一年内实际存在的月份和每月收益，前端按返回月份数量绘制趋势，并按每月 `amount` 在本组数据中的比例换算纵坐标；四个指标卡片按 167pt × 97pt、12pt 圆角、青色渐变和 1pt 边框换算实现。分润流水当前为静态列表，数量多时依赖页面下滑展示，正式联调需支持分页 / 加载更多。未实现：点击收益趋势当月点位展示当月金额气泡 / tooltip，后续交互完善时补齐。 | 用户 2026-06-27 反馈收益趋势月份、流水数量、卡片样式和点位金额提示暂缓 | 待后端确认 / 待实现 |
| `pages/profile/service-center/invite/member-detail/index` | 服务中心 / 我的邀请 - 成员详情查看页 | 静态 UI / 接口 / 交互 | 当前已按用户提供截图调整为深色成员详情卡片，标题为“成员详情查看页”，关闭按钮已移除，页面文字均按 regular 处理；关系网络页一级成员点击已携带 `memberId` 进入本页。当前成员头像、昵称、等级、总邀约、成功转化、转化率、直接贡献收益、团队贡献收益、合计贡献和最近动态均为本地静态兜底；正式联调时需按 `memberId` 从后台获取，不写死最终业务数据。 | 用户 2026-06-27 提供成员详情截图并说明从成员点击进入 | 待后端确认 / 待实现 |
| `pages/profile/member/index`、`pages/profile/member/advanced/index`、`pages/profile/member/premium/index` | 会员中心 - 三档会员 | 静态 UI / 接口 / 交互 | 当前已抽出 `components/member-center` 共享组件，并新增基础会员、高级会员、尊享会员 3 个页面与 3 个编译模式。三档会员的会员等级、页面文案、权益说明、分润比例、雷达适配文案、适配人群、开通权益说明、最近开通会员提示、开通价格、按钮文案和协议提示均应由后台按等级配置返回，支持后台修改；当前仍为本地静态兜底数据。`开始适配`、`立即开通`、服务协议和支付生效流程未接真实接口，接口字段详见 `docs/api-additions.md` 第 50 节。 | 用户 2026-06-26 粘贴 HTML、指定编译模式并反馈页面内容可后台修改 | 待后端确认 / 待实现 |
| `pages/profile/member/index` | 会员中心 - 组局雷达 | 静态 UI | 后续按用户截图继续精修雷达区域：橙色同心圆、虚线圆环、十字轴线、扫描扇区、头像节点位置、中心 `局 / 爱 / 友` 点位，以及下方 `开始适配` 描边按钮需与截图继续对齐；当前版本仅为结构占位。 | 用户 2026-06-26 截图反馈 | 待精修 |
| `pages/profile/member/radar/index`、`pages/profile/member/match/index`、`pages/profile/member/match-info/index`、`pages/profile/member/match-query/index`、`pages/profile/member/match-result/index` | 会员中心 - 人脉雷达 5 页 | 静态 UI / 接口 / 交互 | 当前已按 `E:\项目\03周总\会员中心` 5 个文件夹新增会员中心人脉雷达流程页面，并生成 5 个编译模式：`我的-会员中心-组局雷达`、`我的-会员中心-适配组局`、`我的-会员中心-适配信息`、`我的-会员中心-匹配结果查询页`、`我的-会员中心-匹配结果最终页`。页面使用 `components/member-radar-flow` 共享组件，雷达背景和头像素材已复制到项目内 `pages/profile/member/assets`。点击 `开启适配人脉` 已进入独立搜索页；本地会生成匹配请求，适配信息为空时按全量匹配，有有效字段时按条件匹配。匹配结果查询页的适配行家数量、行家卡片信息、`下一个` 和 `关注` 都需接匹配结果接口；当前仍为静态占位，接口待确认详见 `docs/api-additions.md` 第 51 节。适配信息保存、查看个人主页、重新匹配仍需接真实接口。 | 用户 2026-06-26 指定会员中心 5 个文件夹并要求新增 5 个微信编译模式 | 待后端确认 / 待实现 |
| `pages/profile/member/match-result/index` | 会员中心 - 匹配结果最终页 | 接口 / 交互 / 状态 | 当前已改为单屏完成态，并实现本地兜底的 `重新查看` 返回已扫描行家查询页、`再次重新匹配` 进入搜索页。后续需接真实 `matchId` 和匹配结果 `total`，最终页推荐数量必须来自前面扫描 / 查询结果；`重新查看` 应复用已扫描出的行家结果，不重新扫描；`再次重新匹配` 需调用发起匹配接口，并确认会员权限、匹配次数、扣费 / 扣次数、失败提示和是否覆盖旧 `matchId`。 | 用户 2026-06-26 指定最终页按截图改为单页并要求记录未完成内容 | 待后端确认 / 待实现 |
| `pages/profile/member/match-info/index` | 会员中心 - 适配信息填写 | 接口 / 交互 | 适配信息字段 `地址定位`、`所在行业`、`营收规模`、`感兴趣组局`、`我的资源`、`我的需求`、`近期诉求` 需要补充字段配置、选项列表、定位、表单校验和保存接口；接口待确认详见 `docs/api-additions.md` 第 52 节。 | 用户 2026-06-26 要求记录适配信息填写接口 | 待后端确认 / 待实现 |

## 2026-06-27 我的足迹成就墙

| 页面 | 功能 / 区域 | 类型 | 问题 | 来源 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `pages/profile/footprint/achievements/index` | 我的足迹 - 我的成就墙 | 静态 UI / 接口 / 交互 | 当前已按用户最新截图调整为蓝色游戏母版 + 黑色内容区成就墙页面，并新增微信编译模式“我的-我的足迹-我的成就墙”；个人中心“足迹中心 / 我的成就墙”入口已指向该页。页面保留等级皇冠卡、升级进度、成就分类、已获得 / 未解锁成就网格和赛季信息，当前数据均为本地静态兜底。当前截图仅作为临时参考，后续需要业主确认最终设计后再继续修改 / 精修。正式联调时成就等级、升级所需点数、进度、分类、成就列表、锁定状态、赛季数据和点击后的详情 / 领取行为均需后台返回或确认。 | 用户 2026-06-27 提供“成就墙”HTML 并要求“新加我的成就墙页面，微信编译模式我的-我的足迹-我的成就墙”；用户 2026-06-27 追加截图要求参照截图修改；用户 2026-06-27 要求记录需业主确定设计后再修改 | 待业主确认设计 / 待后端确认 |

## 2026-06-27 资产中心我的订单与物流弹层

| 页面 | 功能 / 区域 | 类型 | 问题 | 来源 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `pages/profile/asset-center/orders/index` | 我的订单 - 查看物流 / 物流详情 | 接口 / 交互 / 状态 | 当前我的订单数据通过 `GET /api/app/profile/points/orders` 按状态 tab 从后台获取，`查看物流` 改为在我的订单当前页打开紧凑物流详情弹层，并调用 `GET /api/app/profile/points/orders/{orderId}/logistics` 获取快递公司、运输单号和轨迹；单独物流详情页已删除，后续不再依赖跳转页查看物流。未完成：正式字段、状态枚举、分页、空态、轨迹排序、无物流 / 物流异常 / 已签收展示、复制失败提示、弹层刷新策略仍需确认；`查看详情` 和 `取消订单` 应跳转独立页面，但页面形态、路由、展示字段、取消流程、取消结果页和取消接口仍需业主确认后实现。 | 用户 2026-06-27 要求订单状态切换生效且订单数据来自后台；用户 2026-06-27 要求查看物流改为当前页弹层并提供紧凑弹窗截图；用户 2026-06-27 说明取消订单和查看详情需记录待业主确认页面；用户 2026-06-27 确认删除单独物流详情页 | 待业主确认 / 待后端联调 |

## 2026-06-27 系统管理我的资料

| 页面 | 功能 / 区域 | 类型 | 问题 | 来源 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `pages/profile/system-management/profile-info/index` | 系统管理 - 我的资料 / 资料设置 | 静态 UI / 接口 / 交互 | 当前已按用户提供的“资料设置”HTML 和截图新增静态走查页，并新增微信编译模式“我的-系统管理-我的资料”；个人中心“系统管理 / 我的资料”入口已指向本页，旧 `pages/profile/match-info/index` 保留为适配信息页。页面展示个人信息、联系方式可见性、企业信息、公开注册业务信息开关、认证中心和隐私提示；点击右上角保存已通过 `PUT /api/app/profile/system-management/profile-info` 将当前页面资料提交后台 / mock，并有保存中防重复状态。仍未完成：真实资料读取、上传头像、编辑字段、联系方式隐私口径、公开注册业务信息、实名认证 / 企业认证状态和认证流程；正式联调时这些数据、字段和状态均需后台返回，前端不写死最终资料。 | 用户 2026-06-27 提供 HTML 和截图并要求“参照这个新建我的资料页，微信编译模式我的-系统管理-我的资料”；用户 2026-06-27 要求保存该页并发送后台 | 待后端确认 / 待实现 |

## 2026-06-27 系统管理技能配置

| 页面 | 功能 / 区域 | 类型 | 问题 | 来源 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `pages/profile/system-management/skill-config/index` | 系统管理 - 技能配置 | 静态 UI / 接口 / 交互 | 当前已按用户提供的“技能配置”HTML 信息结构和后续深色参考截图落地页面，标题“技能配置”，并新增微信编译模式“我的-系统管理-技能配置”；个人中心“系统管理 / 技能配置”入口已指向本页。页面展示当前身份、技能数量限制、每月修改次数、技能槽位、显性技能 / 隐形技能 / 服务案例 tab、技能卡片、定制服务案例和一键解锁提示；点击保存已通过 `PUT /api/app/profile/system-management/skill-config` 提交当前配置到后台 / mock，并有保存中防重复状态。当前已按用户 2026-06-28 提供的响应截图补充本地交互：移除技能确认弹窗、修改次数不足提示弹窗、编辑技能底部面板、解锁新技能确认弹窗和添加技能底部面板；警示 / 倒计时 / 锁图标使用项目内已有素材副本，候选新增技能使用 `📸`、`📋`、`🎪`、`🧠` emoji。仍未完成：真实配置读取、显性 / 隐形技能规则、AI 解锁来源、服务案例绑定、每月修改次数扣减后端规则、添加 / 编辑 / 移除 / 解锁的正式接口和失败提示；正式联调时这些数据、字段和状态均需后台返回，前端不写死最终技能配置。 | 用户 2026-06-27 提供 HTML 并要求“白色母版设计该页，该页标题技能配置，微信模式我的-系统管理-技能配置”；用户 2026-06-27 提供深色参考图并要求参照修改页面；用户 2026-06-28 提供响应功能截图和 emoji 图标 | 待后端确认 / 待实现 |
| `pages/profile/system-management/skill-config/index` | 技能配置 - 隐形技能空态 | 静态 UI / 接口 / 交互 | 当前已按用户 2026-06-28 参考图先关闭默认隐形技能候选；进入“隐形技能”tab 且无隐形技能数据时，只展示锁图标、“暂无隐形技能”和两行说明，不展示隐形技能卡片或一键解锁卡。后续若后台返回 `skillGroups.hidden` 数据，再按技能配置页卡片样式展示并开放解锁建议。 | 用户 2026-06-28 要求“隐形技能先按照图片样式关掉” | 待后端确认 / 待实现 |
| `pages/profile/system-management/skill-config/index` | 技能配置 - 第三个技能槽 | 静态 UI / 接口 / 交互 | 当前第三个技能槽未解锁时仍按参考图显示灰色 `+ / 添加技能`，但点击只提示“请先解锁第三个技能”，不会打开添加面板；点击下方“解锁第三个技能”只解锁槽位，不直接添加技能，解锁后再点击上方槽位才可进入添加技能面板。正式联调时技能槽需返回 `locked` 状态，前端按 `empty + locked` 控制是否允许添加。 | 用户 2026-06-28 说明“上面未解锁的时候不能添加，如果解锁了可以添加”并提供槽位截图 | 待后端确认 / 待实现 |
| `pages/profile/system-management/service-case-detail/index` | 系统管理 - 技能配置 - 服务案例详情 | 静态 UI / 接口 / 交互 | 当前已按用户提供的“服务案例详情页@3x.png”新增深色静态详情页，并新增微信编译模式“我的-系统管理-技能配置-服务案例详情”。页面展示案例图标、标题、日期、人数、评分、玩家评价标签、定制服务详情和参与玩家列表；案例图标不在详情页写死，技能配置页服务案例列表点击时会携带当前案例的 `caseId`、`iconText` 和 `tone` 进入详情页，详情页按同一套 emoji 图标容器渲染，确保图标内容居中且与前页一致。当前服务案例列表已增加右箭头，点击案例会进入本详情页，并按本地兜底数据切换详情内容；仍未接真实案例详情接口、服务案例列表选择、参与玩家完整列表、评分标签和案例详情数据。正式联调时这些字段应由后台返回，前端不写死最终业务数据。 | 用户 2026-06-28 提供服务案例详情参考图并确认只做静态还原、添加对应微信编译模式；用户 2026-06-28 要求服务案例增加右箭头并可查看详情；用户 2026-06-28 确认详情页图标应来自前一页点击案例数据 | 待后端确认 / 待入口联动 |

## 2026-06-28 系统管理屏蔽设置

| 页面 | 功能 / 区域 | 类型 | 问题 | 来源 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `pages/profile/system-management/block-settings/index`、`pages/profile/system-management/protection-mode/index`、`pages/profile/system-management/scene-config/index`、`pages/profile/system-management/whitelist/index`、`pages/profile/system-management/renewal/index`、`pages/profile/system-management/keywords/index`、`pages/profile/system-management/users/index` | 系统管理 - 屏蔽设置 7 页 | 静态 UI / 接口 / 交互 | 当前已按 `E:\项目\03周总\系统管理\03屏蔽设置` 下 00-06 参考代码和截图新增静态走查页，并新增“我的-系统管理-屏蔽设置-首页 / 保护模式 / 分场景配置 / 白名单 / 续期保护期 / 关键词屏蔽 / 用户屏蔽”编译模式；软保护、分场景弹窗、白名单添加弹窗和用户屏蔽添加弹窗通过 query 预览。页面套用 `profile-white-shell` 白色母版导航，内容区按参考图深色风格实现；个人中心“屏蔽设置”入口已进入首页。当前开关、保护模式、分场景选择、白名单添加 / 移除、续期天数、关键词添加 / 删除、用户屏蔽添加 / 解除均为本地静态模拟，未接真实接口或持久化。正式联调时需确认屏蔽总览、保护状态、保护期、场景策略、白名单、关键词、用户屏蔽列表、搜索、保存、解除和冷却期规则等接口字段与失败提示。 | 用户 2026-06-28 提供屏蔽设置资料目录并要求按代码生成页面、对照图片修改、每页生成编译模式；用户补充要求应用白色母版但样式参照代码和图片 | 待后端确认 / 待实现 |
| `pages/profile/system-management/block-settings/index`、`pages/profile/system-management/protection-mode/index`、`pages/profile/system-management/scene-config/index`、`pages/profile/system-management/whitelist/index`、`pages/profile/system-management/renewal/index`、`pages/profile/system-management/keywords/index`、`pages/profile/system-management/users/index` | 屏蔽设置后台数据与规则配置 | 接口 / 配置 | 首页保护用户数、已屏蔽行家数、过滤率、保护期剩余天数、开关状态、当前保护模式、每个场景的过滤策略、白名单行家列表、续期可选天数和价格 / 权益说明、关键词列表、屏蔽用户列表、规则说明标题 / 文案 / 样式以及所有保存后的展示状态，都应从后台返回并支持后台修改；当前页面仅保留静态兜底数据用于走查。后续若后台调整规则说明，需要把对应文案、结构和样式配置一起返回或同步提交给前端，不在前端写死最终规则。 | 用户 2026-06-28 反馈“这里的数字为后台来的可以更换修改”“规则说明可以从后台修改”“后续修改规则说明的话，具体样式由他改的一起提交过来”“这些屏蔽的数字、设置等都是从后台获取的” | 待后端确认 / 待实现 |
| `pages/profile/system-management/protection-mode/index`、`pages/profile/system-management/scene-config/index`、`pages/profile/system-management/whitelist/index`、`pages/profile/system-management/keywords/index`、`pages/profile/system-management/users/index` | 屏蔽设置分页图标 | 素材 / UI | 分页已按用户要求先压缩字号、间距、卡片高度和底部操作区，尽量一屏展示；用户屏蔽、关键词屏蔽等页面图标暂不再由前端自行替换，待用户提供可用图标素材后，需复制到项目内对应 assets 目录并替换引用。 | 用户 2026-06-28 要求分页紧凑展示，并说明图标不要自行绘制、后续由用户提供 | 待用户提供素材 |

## 2026-06-28 系统管理建议反馈

| 页面 | 功能 / 区域 | 类型 | 问题 | 来源 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `pages/profile/system-management/feedback/index`、`pages/profile/system-management/feedback-records/index`、`pages/profile/system-management/feedback-detail/index`、`pages/profile/system-management/feedback-success/index` | 系统管理 - 建议反馈 5 个设计状态 | 静态 UI / 接口 / 交互 | 当前已按 `E:\项目\03周总\系统管理\04建议反馈` 下 `00建议反馈`、`01我的反馈`、`02反馈详情`、`03快捷反馈`、`04反馈成功` 参考代码和截图新增静态走查；`03快捷反馈` 判断为建议反馈首页上的底部弹窗状态，不单独建小程序页面，编译模式通过 `pages/profile/system-management/feedback/index?sheet=quick` 直接打开弹窗。个人中心“建议反馈”入口已进入反馈首页。当前反馈类型选择、关联组局选择、上传截图、语音反馈、提交反馈、反馈记录筛选、详情补充说明、成功页评分和满意点选择均为本地静态 / 提示交互，未接真实接口、文件上传、录音、客服会话或持久化。后续可反馈的局列表、已经反馈的问题列表、各状态数量、反馈回复状态和详情对话都必须从后台获取，前端不写死最终业务数据。正式联调时需确认反馈提交、可反馈的局、记录列表、详情对话、补充说明、满意度评价、截图上传和语音反馈接口字段与失败提示。 | 用户 2026-06-28 提供建议反馈资料目录并要求按代码生成页面、对照图片修改、每页生成编译模式；用户补充要求页面名称使用文件夹名称，并询问快捷反馈弹窗是否单独建页面；用户 2026-06-28 说明可反馈的局、已反馈的问题等都从后台获取 | 待后端确认 / 待实现 |

## 2026-06-28 系统管理举报中心

| 页面 | 功能 / 区域 | 类型 | 问题 | 来源 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `pages/profile/system-management/report-center/index`、`pages/profile/system-management/report-appeals/index`、`pages/profile/system-management/report-records/index`、`pages/profile/system-management/report-detail/index`、`pages/profile/system-management/appeal-detail/index`、`pages/profile/system-management/report-record-detail/index` | 系统管理 - 举报中心 6 页 | 静态 UI / 接口 / 交互 | 当前已按 `E:\项目\03周总\系统管理\06举报中心` 下 6 张参考图和 `新建文本文档.txt` HTML 结构新增静态走查页，并新增“我的-系统管理-举报中心-举报中心 / 我的申诉页 / 申诉处理记录页 / 举报详情页 / 申诉详情页 / 处理记录详情页”编译模式；页面套用 `profile-white-shell` 白色母版导航，内容区按参考图深蓝、荧光绿、圆角卡片和状态标签还原。个人中心“举报中心”入口已进入举报中心首页。当前举报类型选择、举报原因输入、上传证据、提交举报、申诉列表、处理记录筛选、详情查看、撤回申诉和处理评价均为本地静态 / 提示交互，未接真实举报、申诉、证据上传、处理记录、奖励和评价接口。状态成功图标复用项目内已有图标副本，温馨提示和上传加号优先来自用户本地图标目录并复制到页面 assets；若后续用户提供更贴近截图的状态图标，应替换 `report-center/assets` 下对应素材。 | 用户 2026-06-28 提供举报中心资料目录并要求按代码生成页面、应用白色母版、风格样式按代码和图片、图片优先、每页生成编译模式；用户补充要求图标优先选择和图片一样的 emoji，不要先自己画 | 待后端确认 / 待运行截图复核 |
| `pages/profile/system-management/report-center/index`、`pages/profile/system-management/report-appeals/index`、`pages/profile/system-management/report-records/index`、`pages/profile/system-management/report-detail/index`、`pages/profile/system-management/appeal-detail/index`、`pages/profile/system-management/report-record-detail/index` | 举报中心未完成项 | 接口 / 后台 / 样式复核 | 当前只完成前端静态走查和本地交互：举报类型本地选择、被举报人与原因本地输入、证据图片本地选择 / 预览 / 删除、我的申诉与处理记录本地筛选、查看详情跳转、详情页返回列表、撤回申诉 / 评价处理待开发提示。未实现：真实用户搜索与选择、举报提交、证据上传入库、提交成功状态、举报详情按 `reportId` 拉取、我的申诉按状态分页、申诉详情按 `appealId` 拉取、处理记录统计与列表按状态分页、处理记录详情按 `recordId` 拉取、撤回申诉、我要申诉正式流程、处理评价、奖励发放状态、信用分变更、加载 / 空态 / 错误态 / 权限与风控失败提示。样式方面当前先按用户反馈统一使用项目默认 regular 字体并保留现版深色样式；仍需在微信开发者工具逐页截图复核申诉详情、处理记录详情、举报详情的字号、行高、字色、卡片间距、状态图标和底部按钮，与参考图不一致处后续再精修。 | 用户 2026-06-28 连续反馈举报中心按钮、筛选、跳转速度、详情页字体和“处理中”样式，并要求先保存记录未实现功能和样式；用户说明需要接口和后台处理 | 待后端处理 / 待样式复核 |

## 2026-06-28 系统管理签署协议

| 页面 | 功能 / 区域 | 类型 | 问题 | 来源 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `pages/profile/system-management/agreement-sign/index`、`pages/profile/system-management/agreement-detail/index` | 系统管理 - 签署协议 | 静态 UI / 接口 / 交互 | 当前已按 `E:\项目\03周总\系统管理\07协议签署` 下可见的 3 组参考资料新增静态走查：签署协议列表页、协议详情查看页、确认签署弹窗状态；页面套用 `profile-white-shell` 白色母版导航，内容区按参考图深色风格还原，图标素材来自参考 HTML 的 SVG 并复制到项目内 assets。已新增编译模式“我的-系统管理-签署协议-签署协议页 / 协议详情查看页 / 确认签署弹窗页”，个人中心“签署协议”入口已指向列表页。当前协议列表、签署状态、协议详情正文、签署确认和签署结果均为本地静态兜底，未接真实协议列表、协议详情、签署记录和签署提交接口。外部资料目录实际只包含 3 张截图和 3 个 HTML 文件夹，未发现用户所说第 4 页资料；后续需补充第 4 页截图 / HTML 后再新增或精修。 | 用户 2026-06-28 提供协议签署资料目录并要求按代码生成页面、应用白色母版、样式按代码和图片、对照图片核对修改、每页按图片名生成编译模式；资料盘点时实际只发现 3 页 | 待后端确认 / 待第 4 页资料 |

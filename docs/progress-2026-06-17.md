# 2026-06-17 进度记录

## 本次停点

1. 今天停在玩家首页上半部分：母版和启动动画已进入阶段稿，玩家首页已完成 HELLO、玩家信息卡、发起组局 / 局前大厅、地球 online、附近正在发生 tabs 和公共组局卡首版。
2. 继续开发时不要一次性大改下半部分，仍按用户要求“一小段一小段”推进。
3. 地球 online 当前效果已被用户确认“就现在这样就好了”，后续除非用户点名，不要再改它的素材和滚动方式。

## 已完成

1. `components/home-shell/index.*` 作为首页母版外框，顶部品牌仍使用图片 logo，不改文字。
2. `pages/entry/index.*` 做启动动画走查页，并在 `project.private.config.json` 增加启动项。
3. `pages/home/player/index.*` 是独立玩家首页，注册到 `app.json`，内容嵌入 `home-shell` 黑色内容区。
4. 局前大厅图标从用户给的 HTML 资料里抽取成路线图标资产，发起组局 / 局前大厅两张入口卡片已按最新反馈放松文字间距。
5. 地球 online 的附近组局数量和已打卡数量已从 `home.nearbySummary` 映射；mock 当前给 `nearbyGameCount: 12`、`checkedInCount: 8`，正式接口待后端确认。
6. 地球 online 右侧使用 `pages/home/player/assets/earth-map-texture.png`，只让内部地图图案横向滚动，外层球体不滚动；素材已按用户确认改为绿色陆地、蓝色海洋。
7. 附近正在发生标题已右移；`全部 / 附近` tabs 为常规字重、居中显示，默认选中 `全部`，点击切换展示范围。
8. 新增 `components/game-card/index.*` 公共组局卡，当前用于 `附近正在发生` 和 `朋友都在玩`，封面、头像、右侧操作内容都通过数据填充。

## 后续事项

1. 继续从玩家首页 `附近正在发生` 卡片往下精修，不要扩大到行家 / 领路人首页。
2. 公共组局卡的三枚卡通头像原图未在用户指定资料目录中找到；组件已支持 `avatarUrls`，正式阶段等后台返回或补素材。
3. 组局卡封面、参与者头像、操作项字段已补到 `docs/api-additions.md` 待后端确认。
4. 不要重新引入本地路径 `@font-face`，也不要把 `ARIALN_1.ttf` 加回来。

## 关键文件

1. `pages/home/player/index.*`
2. `pages/home/player/assets/*`
3. `components/home-shell/index.*`
4. `components/game-card/index.*`
5. `pages/entry/index.*`
6. `api/mock-data.js`
7. `docs/api-additions.md`
8. `docs/follow-ups.md`

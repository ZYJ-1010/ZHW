# 2026-06-16 进度记录

## 首页母版组件化

1. 新增 `components/home-shell/index.*`，将首页 / 角色页共用母版外框抽成组件。
2. 组件负责顶部品牌栏、在线人数、右上搜索 / 消息 / 头像图标、底部方向键、`首页 / 消息 / 元宇宙 / 地图 / 我的` 圆按钮。
3. 组件中间内容区使用默认 slot，后续玩家、行家、领路人等角色内容应放入 `<home-shell>...</home-shell>`，不要复制母版外框。
4. `pages/home/master/index` 已改为母版组件的走查入口，只传 `onlineText`、`navItems`、`toolbarStyle`，中间保留黑色占位内容。
5. 所有母版按键、方向键和右上图标暂不接真实跳转，统一弹 `功能正在开发中`；后续正式业务行为再在组件 `navtap` 或页面 `handleShellNavTap` 中按 key 分发。

## 字体与资源

1. 小程序不使用本地路径 `@font-face` 加载字体，避免开发者工具报 `do-not-use-local-path`。
2. 玩家首页和母版统一使用系统可识别的 `"Noto Sans SC"` 字体名，并通过字重控制 Regular / Bold 效果。
3. 右上图标资源已复制到 `components/home-shell/assets/`，组件不再依赖 `pages/home/master/assets`。

## 玩家首页独立页

1. 新增独立页面 `pages/home/player/index.*`，通过 `home-shell` 母版组件嵌入玩家首页内容，未混入旧的 `pages/home/index`。
2. `app.json` 已注册 `pages/home/player/index`，`project.private.config.json` 已增加“玩家首页独立页”启动项。
3. 本次字体核对只看玩家首页两段导出：
   - `E:\项目\03周总\首页\html-我的首页上半部分\html\Index.html`
   - `E:\项目\03周总\首页\react-我的首页-下半部分\react\src\styles\Frame2549.css`
4. 玩家首页落地字体统一使用 `"Noto Sans SC"`，不再额外注册本地字体别名。
5. “查看完整榜单”已按最新要求使用常规字重，不再为这一处单独引入额外字体资源。
6. 白板排查：开发者工具报 `pages/home/player/index.wxml not found` 和 `pages/home/player/index.js 已被代码依赖分析忽略`，根因是本地设置 `ignoreDevUnusedFiles` 开启后过滤了新独立页；已在 `project.private.config.json` 改为 `false`。若工具仍沿用缓存，重新编译或重开项目后再选“玩家首页独立页”。

## 后续建议

1. 新增玩家 / 行家 / 领路人内容时，优先做角色内容组件，再塞进 `home-shell` slot。
2. 后续若需要接真实跳转，保留组件外框不动，只处理 `navtap` 的 key：`home`、`message`、`metaverse`、`mine`、`map`、`search`、`comment`、`avatar`、`up`、`left`、`right`、`down`。
3. 不要重新引入本地字体文件或本地路径 `@font-face`；如后续确实需要品牌字体，应先确认小程序包体和加载方案。

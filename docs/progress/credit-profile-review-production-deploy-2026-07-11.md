# 信用、申诉、个人资料与评价生产发布记录 - 2026-07-11

## 发布范围

- 个人资料头像文件归属校验、下载 URL 生成与资料接口回显。
- 取消服务信用规则兜底：玩家取消扣 3 分，行家取消扣 5 分。
- 申诉历史保留已处理、已撤回和信用申诉记录。
- 信用申诉通过后的信用变化按正数保存和展示。
- 评价待办生产文件已与本地目标版本一致，本次未重复覆盖。
- 新增并执行 `db/migrations/000040_cancel_service_credit_rules.sql`。

## 发布方式

- 先下载生产当前目标源码并与本地逐文件对比，仅发布本次存在差异的 3 个 Go 源码文件。
- 生产源码和数据库完整备份目录：`/opt/zhw-mini/.deploy-backups/credit-profile-20260711-2235`。
- 发布前使用生产当前完整源码进行隔离 Docker 镜像构建，构建成功后才替换生产源码。
- 数据库迁移通过 PostgreSQL 标准输入执行，迁移幂等写入或更新 2 条取消服务信用规则。
- 重新构建并启动生产 `go-api` 容器；未重建 PostgreSQL、Redis、Nginx 或后台网页容器。
- 微信小程序前端未上传，由项目负责人通过微信开发者工具处理。

## 验证结果

- 生产信用规则：`player_cancel_service=-3`、`expert_cancel_service=-5`、`low_review=-5`，均启用。
- 生产发布文件 SHA-256 与本地已测试文件一致。
- Go API 容器状态为 `running`，重启次数为 `0`。
- 内网与公网 `GET /api/app/health` 均返回 HTTP 200。
- 信用中心、申诉历史、个人资料和评价接口公网未登录请求均返回 HTTP 401，确认路由在线且受鉴权保护。
- 发布后 Go API 日志未发现 `panic`、`fatal` 或 `error`。

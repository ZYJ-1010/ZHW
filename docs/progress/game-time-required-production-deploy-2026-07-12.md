# 组局时间必填与引荐时间继承生产部署记录 - 2026-07-12

## 发布范围

- 小程序玩家创建组局时，局开始和结束时间改为必填并纳入发布校验。
- 管理后台创建组局新增必填的开始、结束时间控件及先后顺序校验。
- 后端统一要求所有新建组局必须包含有效的开始、结束时间。
- 引荐生成的 replay 组局继承源组局时间。
- 邀请服务时长根据起止时间计算，不再错误保存完整时间区间。

## 生产数据诊断

- 原始玩家局 `92006` 已正确保存 `2026-07-12 09:23` 至 `11:23`。
- replay 局 `92007`、`92008` 的时间在旧版引荐创建流程中丢失。
- 邀请记录曾将完整时间区间错误写入 `service_duration_text`。
- 历史 replay 局 `92007`、`92008` 已从源局 `92006` 回填开始、结束时间。
- 关联邀请 `1` 至 `4` 的预计时间已修正为 `2026-07-12 09:23 - 11:23`，服务时长已修正为 `2小时`。

## 部署结果

- 仅替换并审计了 `game_invite_handler.go`、`games/service.go`、管理后台 `index.html` 与 `main.js`。
- 重建并重启 `go-api`、`admin-web`，两个容器状态均为 `running`，重启次数均为 `0`。
- 内网及公网 `GET /api/app/health` 均返回 HTTP 200，服务状态为 `ok`。
- 管理后台生产源码已确认包含 `startAt`、`endAt` 必填控件。

## 回滚位置

- `/opt/zhw-mini/.deploy-backups/game-time-required-20260712-125044`
- 历史数据更新前备份：`/opt/zhw-mini/.deploy-backups/replay-time-data-20260712-125742/history-before.sql`

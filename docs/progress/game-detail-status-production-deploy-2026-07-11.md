# 组局详情状态与首页入口生产发布记录 - 2026-07-11

## 本次修改

- 组局详情新增后台 `detailDisplay`，统一返回状态文案、报名待审数量、真实组局者评分和主按钮动作。
- 待后台审核不再显示“立即报名”；主按钮按用户身份与 7 个局状态返回报名、审核、开局、协作、确认或评价入口。
- 组局者无真实评价时隐藏评分区域，不再显示 `--`。
- 首页只向创建者本人补充其 `pending_audit` 局，其他玩家的公开首页仍不可见。
- 修复首页 `game-card` 自定义事件路由读取错误，卡片“查看”可以进入局详情。
- 详情页恢复局须知 `1 / 2 / 3`，修正状态行文字和底部工具/按钮对齐。

## 验证与发布

- 前端相关 JavaScript 语法检查通过；页面事件处理器检查无缺失。
- 新增 Go 定向测试覆盖详情接口、7 个主状态、草稿态、角色动作、真实评分与报名 `gameId` 过滤，全部通过。
- `internal/games` 测试通过；完整 `internal/appapi` 套件仍有本分支既有的 10 项无关失败，本次未扩散修改。
- 发布前线上 `game_handler.go`、`home_payload.go` 与指定分支原始文件哈希一致；已建立服务器备份后仅替换这两个文件。
- Go API 镜像构建和容器重建成功；内网与公网 `/api/app/health` 均返回成功。
- 生产库只读确认用户 `91007` 的局 `92004 / 测试01` 当前为 `pending_audit`；未修改数据库数据，无迁移。

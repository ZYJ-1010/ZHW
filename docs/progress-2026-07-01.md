# 2026-07-01 页面死数据清理进度

## 本次完成

- `pages/game/delivery/index.js`：服务交付确认页改为读取 `services/game.getServiceDeliveryDetail`，提醒玩家确认和确认交付走后端提交接口；提交后刷新时保留当前订单上下文。
- `pages/game/expert-cancel/index.js`：行家取消赔付页改为读取 `getExpertCancelPreview`，提交走 `cancelServiceWithCompensation`；取消原因、金额、协议文案和勾选状态不再写死。
- `pages/game/apply/index.js`：申请加入页改为读取当前用户微信授权信息和 `getGameApplyConfig`，提交走 `applyGame`；默认昵称、自我介绍和留言样例已清空。在线人数按用户最新要求暂不清。
- `pages/game/confirm/index.js`：再次组局确认页复核完成，上局信息、邀请对象和快捷话术不再使用页面内样例兜底。
- `pages/game/collaboration/index.js`：局内协作页改为读取 `getGameCollaboration`，任务、成员和聊天消息默认清空；结束本局走后端接口或后台返回路由。
- `pages/game/guide-chat/index.js`：领路人聊天页改为读取 `getGuideChatContext`，确认 / 婉拒和发送消息走后端接口；默认人物、需求、地点和话术样例已清空。
- `pages/game/greet/index.js`：组局打招呼页改为读取 `getGameGreetingContext`，发送消息走后端接口；默认联系人、行家、价格和介绍样例已清空。
- `pages/game/hall-greet/index.js`：大厅打招呼页改为读取 `getHallGreetingContext`，快捷动作和发送消息走后端接口；快捷动作提示不再在页面里写死。
- `pages/game/payment/index.js`：支付页改为读取支付配置接口，金额、分成明细、规则说明和积分说明由后台返回；支付下单不再识别 `mockPayment`。
- `pages/game/audit/index.js`：审核申请列表改为读取 `getGameAudits`，通过、拒绝和批量处理走后端接口；本地四条申请样例已清空。
- `pages/game/audit-detail/index.js`：审核详情页改为读取 `getGameAuditDetail`，通过 / 拒绝走 `respondGameAudit`；玩家、领路人、预算、结算和推荐语样例已清空。
- `pages/game/play-again/index.js`：再玩一局推荐项改为读取 `getPlayAgainOptions`，点击推荐项走后端选择接口或后台路由。
- `pages/game/success-expert/index.js`：行家成功页改为读取 `getExpertSuccess`，三方群、参与人、活动信息和下一步由后台返回。
- `pages/game/referral-record/index.js`：引荐记录改为读取 `getReferralRecords`，统计、tab、记录和动作由后台返回。
- `pages/game/review/index.js`：评价页改为读取 `getGameReviewConfig`，满意度、评价对象、标签和 NPS 配置由后台返回，提交走 `submitGameReview`。
- `pages/game/review-complete/index.js`：评价完成页改为读取 `getReviewCompleteConfig`，权益和后续参与意向由后台返回。
- `pages/game/share/index.js`：活动数据继续读取局详情接口；在线人数按用户最新要求暂不清。
- `pages/game/create/index.js`：清空默认主题、默认押金文案和默认分润模板；接口失败时不再回填押金 / 分润样例。在线人数按用户最新要求暂不清。
- `pages/game/invite/index.js`：发起邀请页接入 `getGameInviteConfig` 和 `createGameInvite`；默认行家、活动类型、预算、需求标题 / 详情和引荐语样例已清空。
- `pages/home/index.js`：申请行家表单清空技能标签、简介、作品集和服务报价预填样例；申请行家操作页接入 `GET /api/app/role-applications/expert/precheck` 候选服务，接口失败时只展示 `待后台返回` 空态。
- `api/modules/role.js`、`services/role.js`：补充行家申请预检接口封装，申请行家配置接口保留参数透传能力。
- `pages/game/applications/index.js`：清空 JS 中的开发占位说明；WXML 内第 3 批占位文案本轮按“不改模板”规则暂不处理。
- `pages/game/guide-progress-detail/index.js`：后端未返回进度详情时改为空详情，不再生成默认进度步骤和业务描述。
- `pages/game/success-guide/index.js`、`pages/game/success-guide/index.wxml`：复核已接 `getGuideSuccess`，成功页主文案、积分奖励、成局历程、参与双方和后续跟进均改由接口返回；接口失败或不返回字段时不回填本地玩家、行家、时间线或奖励样例。
- `pages/game/review-complete/index.js`、`pages/game/review-complete/index.wxml`：评价完成页成功文案、奖励卡、权益和后续参与意向改为读取 `GET /api/app/game-reviews/complete-config`；接口不返回时隐藏奖励卡，不再写死 `+50` 或奖励说明。
- `pages/profile/credit-center/index.js`、`pages/profile/footprint/achievements/index.js`：复核已分别接 `GET /api/app/profile/credit` 和 `GET /api/app/profile/footprint/achievements`；同步修正 follow-up 中的旧静态描述。
- `pages/profile/system-management/skill-config/index.js`：清空默认重置日期 `2026-07-01`；修改次数不足弹窗仅在后台返回重置日期时展示该行。
- `pages/game/hall/index.js`：高级筛选城市不再固定兜底 `上海`，改为从当前接口列表 `cityName` 取可用城市；取不到时保持空态。
- `pages/profile/asset-center/points/index.js`、`pages/profile/asset-center/points/index.wxml`：积分中心剩余规则限制说明、角色示例标题和底部说明改为读取 `GET /api/app/profile/points` 返回字段；接口不返回时隐藏，不再写死本地业务说明。
- `pages/profile/credit-center/index.js`、`pages/profile/credit-center/index.wxml`：信用中心分数卡、状态徽标和底部规则说明改为读取 `GET /api/app/profile/credit` 返回字段；接口不返回时隐藏，不再写死默认 100 分、月度加分和处罚阈值。
- `pages/profile/system-management/report-center/index.js`、`pages/profile/system-management/report-center/index.wxml`、`pages/profile/system-management/report-detail/index.js`、`pages/profile/system-management/report-detail/index.wxml`、`pages/profile/system-management/report-record-detail/index.js`、`pages/profile/system-management/report-record-detail/index.wxml`：举报中心温馨提示、详情状态卡、证据数量、处理结果行和奖励卡改为接口字段；接口不返回时隐藏，不再写死处罚金额、奖励金额或信用分规则。
- `components/member-center/index.js`、`components/member-center/index.wxml`：三档会员改为读取 `GET /api/app/profile/member-center`；清空本地会员价格、权益、人群、开通说明、最近开通人名和协议文案样例。
- `components/member-radar-flow/index.js`：人脉雷达接入概览、适配信息、发起匹配、匹配结果和关注候选接口；清空本地人物、公司地址、匹配数量和固定结果样例。
- `api/modules/profile.js`、`services/profile.js`：补充会员中心和人脉雷达候选接口封装。
- `api/modules/game.js`、`services/game.js`：补充上述页面所需生产接口封装。
- `docs/api-additions.md`：补充 / 更新协作、聊天、打招呼、支付配置和申请行家接口缺口与当前前端接入口径。

## 已验证

- 已执行 `node --check`：`api/modules/game.js`、`services/game.js`、`pages/game/delivery/index.js`、`pages/game/expert-cancel/index.js`、`pages/game/apply/index.js`、`pages/game/confirm/index.js`、`pages/game/collaboration/index.js`、`pages/game/guide-chat/index.js`、`pages/game/greet/index.js`、`pages/game/hall-greet/index.js`、`pages/game/payment/index.js`、`pages/game/audit/index.js`、`pages/game/audit-detail/index.js`、`pages/game/play-again/index.js`、`pages/game/success-expert/index.js`、`pages/game/referral-record/index.js`、`pages/game/review/index.js`、`pages/game/share/index.js`、`pages/game/review-complete/index.js`、`pages/game/create/index.js`、`pages/game/invite/index.js`。
- 已用 `rg` 扫描 `pages/game` JS，除用户要求暂不处理的在线人数外，未发现样例人名、固定金额、`mockPayment` 等明显残留。

## 仍需继续

- 继续走查组局其他剩余子页；`pages/game/applications/index.js` 仍是占位模板，后续若要做真实入局 / 邀约列表，需要允许改 WXML/WXSS 并确认页面归属。
- 本轮未改 WXML/WXSS，因此 `pages/game/invite/index.wxml`、`pages/game/review/index.wxml`、`pages/game/success-expert/index.wxml` 等模板内 placeholder / 固定说明文案仍需在允许改模板时另行处理。
- 继续走查“我的”剩余页：`pages/profile/credit-center/index.js`、`pages/profile/footprint/achievements/index.js`、`pages/profile/system-management/*` 中尚未复核的页面。
- `pages/home/index.js` 的申请行家配置和预检接口仍需后端确认正式返回字段；当前前端已接候选接口并移除页面内预填样例。
- 不动 WXML/WXSS 的前提下，部分模板内静态文案仍需后续确认是否允许改为 JS 绑定，例如协作页 WXML 中的固定副标题。
